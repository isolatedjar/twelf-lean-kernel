%%% Expected outcome: accept
%%% The binding domain of a forall may need to be reduce before it is a sort
%% SKIP: def id — universe-polymorphic (1 params)

%% SKIP: def forallSortWhnf — synth failed: synth: case app not yet supported

%mode declared +N -LS -T -DK -W.
%worlds () (declared _ _ _ _ _).
%unique declared +N -1LS -1T -1DK *W.

%% Translator summary:
%%   - def id: universe polymorphism not yet supported
%%   - def forallSortWhnf: synth failed (synth: case app not yet supported)
