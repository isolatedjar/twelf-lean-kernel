%%% Render-only view of: 121_dup_ind_def
%%% A definition and a constructor with the same name
%% def dup_ind_def
dup_ind_def/render-type : expr =
   (esort (lsucc lzero)).
dup_ind_def/render-value : expr =
   (esort lzero).

%% inductive dup_ind_def (numParams=0, numIndices=0)
dup_ind_def/render-type : expr =
   (esort (lsucc lzero)).

%% ctor dup_ind_def.mk (induct=dup_ind_def, numFields=0)
dup_ind_def_mk/render-type : expr =
   (econst "dup_ind_def" lnil).

%% recursor dup_ind_def.rec (numMotives=1, numMinors=1, k=false)
dup_ind_def_rec/render-type : {u : lvl} expr =
   [u] (eforall (eforall (econst "dup_ind_def" lnil) ([x] (esort u))) ([x] (eforall (eapp x (econst "dup_ind_def.mk" lnil)) ([y] (eforall (econst "dup_ind_def" lnil) ([z] (eapp x z))))))).
%%   rec-rule for dup_ind_def.mk (nfields=0)
dup_ind_def_rec/render-rule0-rhs : {u : lvl} expr =
   [u] (elam (eforall (econst "dup_ind_def" lnil) ([x] (esort u))) ([x] (elam (eapp x (econst "dup_ind_def.mk" lnil)) ([y] y)))).

