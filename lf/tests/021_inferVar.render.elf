%%% Render-only view of: 021_inferVar
%%% Type inference of local variables
%% def inferVar
inferVar/render-type : expr =
   (eforall (esort lzero) ([x] (eforall x ([y] x)))).
inferVar/render-value : expr =
   (elam (esort lzero) ([x] (elam x ([y] y)))).

