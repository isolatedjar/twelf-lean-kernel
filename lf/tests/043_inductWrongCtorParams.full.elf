%%% Expected outcome: reject
%%% An inductive with a constructor with wrong parameters
%% axiom aProp
aProp/type-wf :
   defeq (esort lzero) (esort lzero) (esort (lsucc lzero))
   = (defeq/sort-refl-at lzero).

aProp/decl : declared "aProp" lnil
   (esort lzero)
   ax
   (dkind-ok/ax aProp/type-wf).

%% SKIP: ctor inductWrongCtorParams.mk — result-type param arg #1 should be bvar(0) (the param binder), got const


%% Translator summary:
%%   - ctor inductWrongCtorParams.mk: result-type param arg #1 should be bvar(0) (the param binder), got const
