%%% Expected outcome: reject
%%% A constructor with an unexpected occurrence of the type in index position of a return type.
%% axiom aProp
aProp/type-wf :
   defeq (esort lzero) (esort lzero) (esort (lsucc lzero))
   = (defeq/sort-refl-at lzero).

aProp/decl : declared "aProp" lnil
   (esort lzero)
   ax
   (dkind-ok/ax aProp/type-wf).

%% SKIP: ctor inductInIndex.mk — result-type index arg #1 mentions the inductive "inductInIndex" (not allowed in index position)


%% Translator summary:
%%   - ctor inductInIndex.mk: result-type index arg #1 mentions the inductive "inductInIndex" (not allowed in index position)
