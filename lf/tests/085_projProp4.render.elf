%%% Render-only view of: 085_projProp4
%%% Projecting out of a proposition
%%% 
%%% The lean kernel disallows data projections out of propositional structures.
%% inductive PUnit
%%% HOLE
PUnit/type-wf-sort : lvl.

%%% HOLE
PUnit/type-wf : of (esort (lvar liz)) (esort PUnit/type-wf-sort).

%%% HOLE
PUnit/ends-in-sort : ends-in-sort (esort (lvar liz)).

PUnit/rec-name : name "PUnit.rec" (is-rec-for "PUnit").

PUnit/name : name "PUnit" (is-decl (esort (lvar liz)) indt).

PUnit/decl : declared "PUnit" (esort (lvar liz)) indt
   = declared/ok PUnit/name (dkind-ok/indt PUnit/type-wf PUnit/ends-in-sort).

%% ctor PUnit.unit (of PUnit)
%%% HOLE
PUnit_unit/type-wf-sort : lvl.

%%% HOLE
PUnit_unit/type-wf : of (econst "PUnit" (lcons (lvar liz) lnil)) (esort PUnit_unit/type-wf-sort).

%%% HOLE
PUnit_unit/positivity : ctor-positive "PUnit" (lcons (lvar liz) lnil) (econst "PUnit" (lcons (lvar liz) lnil)).

PUnit_unit/eisl : ends-in-sort-with-level (esort (lvar liz)) (lvar liz)
   = ends-in-sort-with-level/sort.

%%% HOLE: PUnit_unit/fuo — prover for field-universes-ok-skip-params not yet implemented
PUnit_unit/fuo : field-universes-ok-skip-params liz (econst "PUnit" (lcons (lvar liz) lnil)) (lvar liz).

PUnit_unit/name : name "PUnit.unit" (is-decl (econst "PUnit" (lcons (lvar liz) lnil)) ctor).

PUnit_unit/decl : declared "PUnit.unit" (econst "PUnit" (lcons (lvar liz) lnil)) ctor
   = declared/ok PUnit_unit/name (dkind-ok/ctor PUnit_unit/type-wf PUnit_unit/positivity PUnit/decl PUnit_unit/eisl PUnit_unit/fuo).

%% recursor PUnit.rec
%%% HOLE
PUnit_rec/type-wf-sort : lvl.

%%% HOLE
PUnit_rec/type-wf : of
  (eforall (eforall (econst "PUnit" (lcons (lvar (lis liz)) lnil)) ([x] (esort (lvar liz))))
    ([x] (eforall (eapp x (econst "PUnit.unit" (lcons (lvar (lis liz)) lnil))) ([y] (eforall (econst "PUnit" (lcons (lvar (lis liz)) lnil)) ([z] (eapp x z)))))))
  (esort PUnit_rec/type-wf-sort).

PUnit_rec/decl : declared "PUnit.rec"
  (eforall (eforall (econst "PUnit" (lcons (lvar (lis liz)) lnil)) ([x] (esort (lvar liz))))
    ([x] (eforall (eapp x (econst "PUnit.unit" (lcons (lvar (lis liz)) lnil))) ([y] (eforall (econst "PUnit" (lcons (lvar (lis liz)) lnil)) ([z] (eapp x z)))))))
  irec
   = declared/ok-irec PUnit/rec-name (dkind-ok/irec PUnit_rec/type-wf).

%% inductive Eq
%%% HOLE
Eq/type-wf-sort : lvl.

%%% HOLE
Eq/type-wf : of (eforall (esort (lvar liz)) ([x] (eforall x ([y] (eforall x ([z] (esort lzero))))))) (esort Eq/type-wf-sort).

%%% HOLE
Eq/ends-in-sort : ends-in-sort (eforall (esort (lvar liz)) ([x] (eforall x ([y] (eforall x ([z] (esort lzero))))))).

Eq/rec-name : name "Eq.rec" (is-rec-for "Eq").

Eq/name : name "Eq" (is-decl (eforall (esort (lvar liz)) ([x] (eforall x ([y] (eforall x ([z] (esort lzero))))))) indt).

Eq/decl : declared "Eq" (eforall (esort (lvar liz)) ([x] (eforall x ([y] (eforall x ([z] (esort lzero))))))) indt
   = declared/ok Eq/name (dkind-ok/indt Eq/type-wf Eq/ends-in-sort).

%% ctor Eq.refl (of Eq)
%%% HOLE
Eq_refl/type-wf-sort : lvl.

%%% HOLE
Eq_refl/type-wf : of (eforall (esort (lvar liz)) ([x] (eforall x ([y] (eapp (eapp (eapp (econst "Eq" (lcons (lvar liz) lnil)) x) y) y))))) (esort Eq_refl/type-wf-sort).

%%% HOLE
Eq_refl/positivity : ctor-positive "Eq" (lcons (lvar liz) lnil) (eforall (esort (lvar liz)) ([x] (eforall x ([y] (eapp (eapp (eapp (econst "Eq" (lcons (lvar liz) lnil)) x) y) y))))).

Eq_refl/eisl : ends-in-sort-with-level (eforall (esort (lvar liz)) ([x] (eforall x ([y] (eforall x ([z] (esort lzero))))))) lzero
   = (ends-in-sort-with-level/forall
       ([x]
         (ends-in-sort-with-level/forall
           ([y]
             (ends-in-sort-with-level/forall
               ([z] ends-in-sort-with-level/sort)))))).

%%% HOLE: Eq_refl/fuo — prover for field-universes-ok-skip-params not yet implemented
Eq_refl/fuo : field-universes-ok-skip-params (lis (lis liz)) (eforall (esort (lvar liz)) ([x] (eforall x ([y] (eapp (eapp (eapp (econst "Eq" (lcons (lvar liz) lnil)) x) y) y))))) lzero.

Eq_refl/name : name "Eq.refl" (is-decl (eforall (esort (lvar liz)) ([x] (eforall x ([y] (eapp (eapp (eapp (econst "Eq" (lcons (lvar liz) lnil)) x) y) y))))) ctor).

Eq_refl/decl : declared "Eq.refl" (eforall (esort (lvar liz)) ([x] (eforall x ([y] (eapp (eapp (eapp (econst "Eq" (lcons (lvar liz) lnil)) x) y) y))))) ctor
   = declared/ok Eq_refl/name (dkind-ok/ctor Eq_refl/type-wf Eq_refl/positivity Eq/decl Eq_refl/eisl Eq_refl/fuo).

%% recursor Eq.rec
%%% HOLE
Eq_rec/type-wf-sort : lvl.

%%% HOLE
Eq_rec/type-wf : of
  (eforall (esort (lvar (lis liz)))
    ([x]
      (eforall x
        ([y]
          (eforall (eforall x ([z] (eforall (eapp (eapp (eapp (econst "Eq" (lcons (lvar (lis liz)) lnil)) x) y) z) ([w] (esort (lvar liz))))))
            ([z]
              (eforall (eapp (eapp z y) (eapp (eapp (econst "Eq.refl" (lcons (lvar (lis liz)) lnil)) x) y))
                ([w] (eforall x ([x1] (eforall (eapp (eapp (eapp (econst "Eq" (lcons (lvar (lis liz)) lnil)) x) y) x1) ([y1] (eapp (eapp z x1) y1)))))))))))))
  (esort Eq_rec/type-wf-sort).

Eq_rec/decl : declared "Eq.rec"
  (eforall (esort (lvar (lis liz)))
    ([x]
      (eforall x
        ([y]
          (eforall (eforall x ([z] (eforall (eapp (eapp (eapp (econst "Eq" (lcons (lvar (lis liz)) lnil)) x) y) z) ([w] (esort (lvar liz))))))
            ([z]
              (eforall (eapp (eapp z y) (eapp (eapp (econst "Eq.refl" (lcons (lvar (lis liz)) lnil)) x) y))
                ([w] (eforall x ([x1] (eforall (eapp (eapp (eapp (econst "Eq" (lcons (lvar (lis liz)) lnil)) x) y) x1) ([y1] (eapp (eapp z x1) y1)))))))))))))
  irec
   = declared/ok-irec Eq/rec-name (dkind-ok/irec Eq_rec/type-wf).

%% inductive PropStructure
%%% HOLE
PropStructure/type-wf-sort : lvl.

%%% HOLE
PropStructure/type-wf : of (esort lzero) (esort PropStructure/type-wf-sort).

%%% HOLE
PropStructure/ends-in-sort : ends-in-sort (esort lzero).

PropStructure/rec-name : name "PropStructure.rec" (is-rec-for "PropStructure").

PropStructure/name : name "PropStructure" (is-decl (esort lzero) indt).

PropStructure/decl : declared "PropStructure" (esort lzero) indt
   = declared/ok PropStructure/name (dkind-ok/indt PropStructure/type-wf PropStructure/ends-in-sort).

%% ctor PropStructure.mk (of PropStructure)
%%% HOLE
PropStructure_mk/type-wf-sort : lvl.

%%% HOLE
PropStructure_mk/type-wf : of
  (eforall (econst "PUnit" (lcons (lvar liz) lnil))
    ([x]
      (eforall (econst "PUnit" (lcons (lvar (lis liz)) lnil))
        ([y]
          (eforall (econst "PUnit" (lcons (lvar liz) lnil))
            ([z]
              (eforall (econst "PUnit" (lcons (lvar (lis liz)) lnil))
                ([w]
                  (eforall (eapp (eapp (eapp (econst "Eq" (lcons (lvar (lis liz)) lnil)) (econst "PUnit" (lcons (lvar (lis liz)) lnil))) w) w)
                    ([x1] (eforall (econst "PUnit" (lcons (lvar liz) lnil)) ([y1] (econst "PropStructure" (lcons (lvar liz) (lcons (lvar (lis liz)) lnil)))))))))))))))
  (esort PropStructure_mk/type-wf-sort).

%%% HOLE
PropStructure_mk/positivity : ctor-positive "PropStructure" (lcons (lvar liz) (lcons (lvar (lis liz)) lnil)) (eforall (econst "PUnit" (lcons (lvar liz) lnil)) ([x] (eforall (econst "PUnit" (lcons (lvar (lis liz)) lnil)) ([y] (eforall (econst "PUnit" (lcons (lvar liz) lnil)) ([z] (eforall (econst "PUnit" (lcons (lvar (lis liz)) lnil)) ([w] (eforall (eapp (eapp (eapp (econst "Eq" (lcons (lvar (lis liz)) lnil)) (econst "PUnit" (lcons (lvar (lis liz)) lnil))) w) w) ([x1] (eforall (econst "PUnit" (lcons (lvar liz) lnil)) ([y1] (econst "PropStructure" (lcons (lvar liz) (lcons (lvar (lis liz)) lnil))))))))))))))).

PropStructure_mk/eisl : ends-in-sort-with-level (esort lzero) lzero
   = ends-in-sort-with-level/sort.

%%% HOLE: PropStructure_mk/fuo — prover for field-universes-ok-skip-params not yet implemented
PropStructure_mk/fuo : field-universes-ok-skip-params liz (eforall (econst "PUnit" (lcons (lvar liz) lnil)) ([x] (eforall (econst "PUnit" (lcons (lvar (lis liz)) lnil)) ([y] (eforall (econst "PUnit" (lcons (lvar liz) lnil)) ([z] (eforall (econst "PUnit" (lcons (lvar (lis liz)) lnil)) ([w] (eforall (eapp (eapp (eapp (econst "Eq" (lcons (lvar (lis liz)) lnil)) (econst "PUnit" (lcons (lvar (lis liz)) lnil))) w) w) ([x1] (eforall (econst "PUnit" (lcons (lvar liz) lnil)) ([y1] (econst "PropStructure" (lcons (lvar liz) (lcons (lvar (lis liz)) lnil))))))))))))))) lzero.

PropStructure_mk/name : name "PropStructure.mk" (is-decl
  (eforall (econst "PUnit" (lcons (lvar liz) lnil))
    ([x]
      (eforall (econst "PUnit" (lcons (lvar (lis liz)) lnil))
        ([y]
          (eforall (econst "PUnit" (lcons (lvar liz) lnil))
            ([z]
              (eforall (econst "PUnit" (lcons (lvar (lis liz)) lnil))
                ([w]
                  (eforall (eapp (eapp (eapp (econst "Eq" (lcons (lvar (lis liz)) lnil)) (econst "PUnit" (lcons (lvar (lis liz)) lnil))) w) w)
                    ([x1] (eforall (econst "PUnit" (lcons (lvar liz) lnil)) ([y1] (econst "PropStructure" (lcons (lvar liz) (lcons (lvar (lis liz)) lnil)))))))))))))))
  ctor).

PropStructure_mk/decl : declared "PropStructure.mk"
  (eforall (econst "PUnit" (lcons (lvar liz) lnil))
    ([x]
      (eforall (econst "PUnit" (lcons (lvar (lis liz)) lnil))
        ([y]
          (eforall (econst "PUnit" (lcons (lvar liz) lnil))
            ([z]
              (eforall (econst "PUnit" (lcons (lvar (lis liz)) lnil))
                ([w]
                  (eforall (eapp (eapp (eapp (econst "Eq" (lcons (lvar (lis liz)) lnil)) (econst "PUnit" (lcons (lvar (lis liz)) lnil))) w) w)
                    ([x1] (eforall (econst "PUnit" (lcons (lvar liz) lnil)) ([y1] (econst "PropStructure" (lcons (lvar liz) (lcons (lvar (lis liz)) lnil)))))))))))))))
  ctor
   = declared/ok PropStructure_mk/name (dkind-ok/ctor PropStructure_mk/type-wf PropStructure_mk/positivity PropStructure/decl PropStructure_mk/eisl PropStructure_mk/fuo).

%% recursor PropStructure.rec
%%% HOLE
PropStructure_rec/type-wf-sort : lvl.

%%% HOLE
PropStructure_rec/type-wf : of
  (eforall (eforall (econst "PropStructure" (lcons (lvar liz) (lcons (lvar (lis liz)) lnil))) ([x] (esort lzero)))
    ([x]
      (eforall (eforall (econst "PUnit" (lcons (lvar liz) lnil))
        ([y]
          (eforall (econst "PUnit" (lcons (lvar (lis liz)) lnil))
            ([z]
              (eforall (econst "PUnit" (lcons (lvar liz) lnil))
                ([w]
                  (eforall (econst "PUnit" (lcons (lvar (lis liz)) lnil))
                    ([x1]
                      (eforall (eapp (eapp (eapp (econst "Eq" (lcons (lvar (lis liz)) lnil)) (econst "PUnit" (lcons (lvar (lis liz)) lnil))) x1) x1)
                        ([y1]
                          (eforall (econst "PUnit" (lcons (lvar liz) lnil))
                            ([z1] (eapp x (eapp (eapp (eapp (eapp (eapp (eapp (econst "PropStructure.mk" (lcons (lvar liz) (lcons (lvar (lis liz)) lnil))) y) z) w) x1) y1) z1))))))))))))))
        ([y] (eforall (econst "PropStructure" (lcons (lvar liz) (lcons (lvar (lis liz)) lnil))) ([z] (eapp x z)))))))
  (esort PropStructure_rec/type-wf-sort).

PropStructure_rec/decl : declared "PropStructure.rec"
  (eforall (eforall (econst "PropStructure" (lcons (lvar liz) (lcons (lvar (lis liz)) lnil))) ([x] (esort lzero)))
    ([x]
      (eforall (eforall (econst "PUnit" (lcons (lvar liz) lnil))
        ([y]
          (eforall (econst "PUnit" (lcons (lvar (lis liz)) lnil))
            ([z]
              (eforall (econst "PUnit" (lcons (lvar liz) lnil))
                ([w]
                  (eforall (econst "PUnit" (lcons (lvar (lis liz)) lnil))
                    ([x1]
                      (eforall (eapp (eapp (eapp (econst "Eq" (lcons (lvar (lis liz)) lnil)) (econst "PUnit" (lcons (lvar (lis liz)) lnil))) x1) x1)
                        ([y1]
                          (eforall (econst "PUnit" (lcons (lvar liz) lnil))
                            ([z1] (eapp x (eapp (eapp (eapp (eapp (eapp (eapp (econst "PropStructure.mk" (lcons (lvar liz) (lcons (lvar (lis liz)) lnil))) y) z) w) x1) y1) z1))))))))))))))
        ([y] (eforall (econst "PropStructure" (lcons (lvar liz) (lcons (lvar (lis liz)) lnil))) ([z] (eapp x z)))))))
  irec
   = declared/ok-irec PropStructure/rec-name (dkind-ok/irec PropStructure_rec/type-wf).

%% def projProp4
%%% HOLE
projProp4/type-wf-sort : lvl.

%%% HOLE
projProp4/type-wf : of (eforall (econst "PropStructure" (lcons lzero (lcons (lsucc lzero) lnil))) ([x] (econst "PUnit" (lcons (lsucc lzero) lnil)))) (esort projProp4/type-wf-sort).

%%% HOLE
projProp4/value-typed : of
  (elam (econst "PropStructure" (lcons lzero (lcons (lsucc lzero) lnil))) ([x] (eproj "PropStructure" 3 x)))
  (eforall (econst "PropStructure" (lcons lzero (lcons (lsucc lzero) lnil))) ([x] (econst "PUnit" (lcons (lsucc lzero) lnil)))).

projProp4/name : name "projProp4" (is-decl
  (eforall (econst "PropStructure" (lcons lzero (lcons (lsucc lzero) lnil))) ([x] (econst "PUnit" (lcons (lsucc lzero) lnil))))
  (defn (elam (econst "PropStructure" (lcons lzero (lcons (lsucc lzero) lnil))) ([x] (eproj "PropStructure" 3 x))))).

projProp4/decl : declared "projProp4"
  (eforall (econst "PropStructure" (lcons lzero (lcons (lsucc lzero) lnil))) ([x] (econst "PUnit" (lcons (lsucc lzero) lnil))))
  (defn (elam (econst "PropStructure" (lcons lzero (lcons (lsucc lzero) lnil))) ([x] (eproj "PropStructure" 3 x))))
   = declared/ok projProp4/name (dkind-ok/defn projProp4/type-wf projProp4/value-typed).
