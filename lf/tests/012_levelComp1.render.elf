%%% Render-only view of: 012_levelComp1
%%% Some level computation
%% def levelComp1
levelComp1/render-type : expr =
   (esort (lsucc lzero)).
levelComp1/render-value : expr =
   (esort (limax (lsucc lzero) lzero)).

