%%% Render-only view of: 051_predWithTypeField
%%% An inductive proposition can have constructors with fields of arbitrary level.
%% inductive PredWithTypeField
%%% HOLE
PredWithTypeField/type-wf : defeq (esort lzero) (esort lzero) (esort lzero).

%%% HOLE
PredWithTypeField/ends-in-sort : ends-in-sort (esort lzero).

PredWithTypeField/decl : declared "PredWithTypeField" lnil
   (esort lzero)
   indt
   (dkind-ok/indt PredWithTypeField/type-wf PredWithTypeField/ends-in-sort).

%% ctor PredWithTypeField.mk (of PredWithTypeField)
%%% HOLE
PredWithTypeField_mk/type-wf : defeq (eforall (esort (lsucc lzero)) ([x] (econst "PredWithTypeField" lnil))) (eforall (esort (lsucc lzero)) ([x] (econst "PredWithTypeField" lnil))) (esort lzero).

%%% HOLE
PredWithTypeField_mk/positivity : ctor-positive "PredWithTypeField" lnil (eforall (esort (lsucc lzero)) ([x] (econst "PredWithTypeField" lnil))).

PredWithTypeField_mk/decl : declared "PredWithTypeField.mk" lnil
   (eforall (esort (lsucc lzero)) ([x] (econst "PredWithTypeField" lnil)))
   ctor
   (dkind-ok/ctor PredWithTypeField_mk/type-wf PredWithTypeField_mk/positivity).

%% recursor PredWithTypeField.rec
%%% HOLE
PredWithTypeField_rec/type-wf : defeq (eforall (eforall (econst "PredWithTypeField" lnil) ([x] (esort lzero))) ([x] (eforall (eforall (esort (lsucc lzero)) ([y] (eapp x (eapp (econst "PredWithTypeField.mk" lnil) y)))) ([y] (eforall (econst "PredWithTypeField" lnil) ([z] (eapp x z))))))) (eforall (eforall (econst "PredWithTypeField" lnil) ([x] (esort lzero))) ([x] (eforall (eforall (esort (lsucc lzero)) ([y] (eapp x (eapp (econst "PredWithTypeField.mk" lnil) y)))) ([y] (eforall (econst "PredWithTypeField" lnil) ([z] (eapp x z))))))) (esort lzero).

PredWithTypeField_rec/decl : declared "PredWithTypeField.rec" lnil
   (eforall (eforall (econst "PredWithTypeField" lnil) ([x] (esort lzero))) ([x] (eforall (eforall (esort (lsucc lzero)) ([y] (eapp x (eapp (econst "PredWithTypeField.mk" lnil) y)))) ([y] (eforall (econst "PredWithTypeField" lnil) ([z] (eapp x z)))))))
   irec
   (dkind-ok/irec PredWithTypeField_rec/type-wf).

%% def predWithTypeField
%%% HOLE
predWithTypeField/type-wf : defeq (esort lzero) (esort lzero) (esort lzero).

%%% HOLE
predWithTypeField/value-typed : defeq (econst "PredWithTypeField" lnil) (econst "PredWithTypeField" lnil) (esort lzero).

predWithTypeField/decl : declared "predWithTypeField" lnil
   (esort lzero)
   (defn (econst "PredWithTypeField" lnil))
   (dkind-ok/defn predWithTypeField/type-wf predWithTypeField/value-typed).

