%%% Render-only view of: 040_inductBadNonSort2
%%% Another inductive type with a non-sort type
%% axiom aType
aType/render-type : expr =
   (esort (lsucc lzero)).

%% inductive inductBadNonSort2 (numParams=0, numIndices=0)
inductBadNonSort2/render-type : expr =
   (econst "aType" lnil).

