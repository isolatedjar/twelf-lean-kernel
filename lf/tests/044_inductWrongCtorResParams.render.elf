%%% Render-only view of: 044_inductWrongCtorResParams
%%% An inductive with a constructor with wrong parameters in result (they are swapped)
%% inductive inductWrongCtorResParams
%%% HOLE
inductWrongCtorResParams/type-wf-sort : lvl.

%%% HOLE
inductWrongCtorResParams/type-wf : defeq (eforall (esort lzero) ([x] (eforall (esort lzero) ([y] (esort (lsucc lzero)))))) (eforall (esort lzero) ([x] (eforall (esort lzero) ([y] (esort (lsucc lzero)))))) (esort inductWrongCtorResParams/type-wf-sort).

%%% HOLE
inductWrongCtorResParams/ends-in-sort : ends-in-sort (eforall (esort lzero) ([x] (eforall (esort lzero) ([y] (esort (lsucc lzero)))))).

inductWrongCtorResParams/name : name "inductWrongCtorResParams" (is-decl lnil (eforall (esort lzero) ([x] (eforall (esort lzero) ([y] (esort (lsucc lzero)))))) indt).

inductWrongCtorResParams/decl : declared "inductWrongCtorResParams" lnil
   (eforall (esort lzero) ([x] (eforall (esort lzero) ([y] (esort (lsucc lzero))))))
   indt
   inductWrongCtorResParams/name
   (dkind-ok/indt inductWrongCtorResParams/type-wf inductWrongCtorResParams/ends-in-sort).

%% ctor inductWrongCtorResParams.mk (of inductWrongCtorResParams)
%%% HOLE
inductWrongCtorResParams_mk/type-wf-sort : lvl.

%%% HOLE
inductWrongCtorResParams_mk/type-wf : defeq (eforall (esort lzero) ([x] (eforall (esort lzero) ([y] (eapp (eapp (econst "inductWrongCtorResParams" lnil) y) x))))) (eforall (esort lzero) ([x] (eforall (esort lzero) ([y] (eapp (eapp (econst "inductWrongCtorResParams" lnil) y) x))))) (esort inductWrongCtorResParams_mk/type-wf-sort).

%%% HOLE
inductWrongCtorResParams_mk/positivity : ctor-positive "inductWrongCtorResParams" lnil (eforall (esort lzero) ([x] (eforall (esort lzero) ([y] (eapp (eapp (econst "inductWrongCtorResParams" lnil) y) x))))).

inductWrongCtorResParams_mk/name : name "inductWrongCtorResParams.mk" (is-decl lnil (eforall (esort lzero) ([x] (eforall (esort lzero) ([y] (eapp (eapp (econst "inductWrongCtorResParams" lnil) y) x))))) ctor).

inductWrongCtorResParams_mk/decl : declared "inductWrongCtorResParams.mk" lnil
   (eforall (esort lzero) ([x] (eforall (esort lzero) ([y] (eapp (eapp (econst "inductWrongCtorResParams" lnil) y) x)))))
   ctor
   inductWrongCtorResParams_mk/name
   (dkind-ok/ctor inductWrongCtorResParams_mk/type-wf inductWrongCtorResParams_mk/positivity).

%% recursor inductWrongCtorResParams.rec
%%% HOLE
inductWrongCtorResParams_rec/type-wf-sort : lvl.

%%% HOLE
inductWrongCtorResParams_rec/type-wf : defeq (esort lzero) (esort lzero) (esort inductWrongCtorResParams_rec/type-wf-sort).

inductWrongCtorResParams_rec/name : name "inductWrongCtorResParams.rec" (is-decl lnil (esort lzero) irec).

inductWrongCtorResParams_rec/decl : declared "inductWrongCtorResParams.rec" lnil
   (esort lzero)
   irec
   inductWrongCtorResParams_rec/name
   (dkind-ok/irec inductWrongCtorResParams_rec/type-wf).

