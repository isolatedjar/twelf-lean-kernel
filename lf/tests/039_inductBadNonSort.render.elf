%%% Render-only view of: 039_inductBadNonSort
%%% An inductive type with a non-sort type
%% def constType
%%% HOLE
constType/type-wf-sort : lvl.

%%% HOLE
constType/type-wf : defeq (eforall (esort (lsucc lzero)) ([x] (eforall (esort (lsucc lzero)) ([y] (esort (lsucc lzero)))))) (eforall (esort (lsucc lzero)) ([x] (eforall (esort (lsucc lzero)) ([y] (esort (lsucc lzero)))))) (esort constType/type-wf-sort).

%%% HOLE
constType/value-typed : defeq (elam (esort (lsucc lzero)) ([x] (elam (esort (lsucc lzero)) ([y] x)))) (elam (esort (lsucc lzero)) ([x] (elam (esort (lsucc lzero)) ([y] x)))) (eforall (esort (lsucc lzero)) ([x] (eforall (esort (lsucc lzero)) ([y] (esort (lsucc lzero)))))).

constType/name : name "constType" (is-decl lnil (eforall (esort (lsucc lzero)) ([x] (eforall (esort (lsucc lzero)) ([y] (esort (lsucc lzero)))))) (defn (elam (esort (lsucc lzero)) ([x] (elam (esort (lsucc lzero)) ([y] x)))))).

constType/decl : declared "constType" lnil
   (eforall (esort (lsucc lzero)) ([x] (eforall (esort (lsucc lzero)) ([y] (esort (lsucc lzero))))))
   (defn (elam (esort (lsucc lzero)) ([x] (elam (esort (lsucc lzero)) ([y] x)))))
   constType/name
   (dkind-ok/defn constType/type-wf constType/value-typed).

%% inductive inductBadNonSort
%%% HOLE
inductBadNonSort/type-wf-sort : lvl.

%%% HOLE
inductBadNonSort/type-wf : defeq (econst "constType" lnil) (econst "constType" lnil) (esort inductBadNonSort/type-wf-sort).

%%% HOLE
inductBadNonSort/ends-in-sort : ends-in-sort (econst "constType" lnil).

inductBadNonSort/name : name "inductBadNonSort" (is-decl lnil (econst "constType" lnil) indt).

inductBadNonSort/decl : declared "inductBadNonSort" lnil
   (econst "constType" lnil)
   indt
   inductBadNonSort/name
   (dkind-ok/indt inductBadNonSort/type-wf inductBadNonSort/ends-in-sort).

