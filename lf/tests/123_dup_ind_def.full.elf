%%% Expected outcome: reject
%%% A definition and a constructor with the same name
%% def dup_ind_def
dup_ind_def/type-wf-sort : lvl
   = (lsucc (lsucc lzero)).

dup_ind_def/type-wf : of (esort (lsucc lzero)) (esort dup_ind_def/type-wf-sort)
   = defeq/sort-refl.

dup_ind_def/value-typed : of (esort lzero) (esort (lsucc lzero))
   = defeq/sort-refl.

dup_ind_def/name : name "dup_ind_def" (is-decl (esort (lsucc lzero)) (defn (esort lzero))).

dup_ind_def/decl : declared "dup_ind_def" (esort (lsucc lzero)) (defn (esort lzero))
   = declared/ok dup_ind_def/name dkind-non-inductive/defn (dkind-ok/defn dup_ind_def/type-wf dup_ind_def/value-typed).

%% inductive dup_ind_def
dup_ind_def/type-wf-sort : lvl
   = (lsucc (lsucc lzero)).

dup_ind_def/type-wf : of (esort (lsucc lzero)) (esort dup_ind_def/type-wf-sort)
   = defeq/sort-refl.

dup_ind_def/ends-in-sort : ends-in-sort (esort (lsucc lzero))
   = ends-in-sort/sort.

dup_ind_def/rec-name : name "dup_ind_def.rec" (is-rec-for "dup_ind_def").

dup_ind_def/eisl : ends-in-sort-with-level (esort (lsucc lzero)) (lsucc lzero)
   = ends-in-sort-with-level/sort.

dup_ind_def/clf : count-leading-foralls (esort (lsucc lzero)) liz
   = count-leading-foralls/zero.

dup_ind_def/cnd : cnames-distinct (ccons "dup_ind_def.mk" cnil)
   = (cnames-distinct/cons cnames-distinct/nil all-string-neq/nil).

dup_ind_def/name : name "dup_ind_def" (is-decl (esort (lsucc lzero)) (indt (ccons "dup_ind_def.mk" cnil) liz)).

dup_ind_def/decl : declared "dup_ind_def" (esort (lsucc lzero)) (indt (ccons "dup_ind_def.mk" cnil) liz)
   = declared/ok-indt dup_ind_def/name dup_ind_def/rec-name dup_ind_def/clf dup_ind_def/cnd (dkind-ok/indt dup_ind_def/type-wf dup_ind_def/ends-in-sort).

%% ctor dup_ind_def.mk (of dup_ind_def)
dup_ind_def_mk/type-wf-sort : lvl
   = (lsucc lzero).

dup_ind_def_mk/type-wf : of (econst "dup_ind_def" lnil) (esort dup_ind_def_mk/type-wf-sort)
   = (defeq/const
       dup_ind_def/decl
       (inst-expr/sort (lvl-inst/succ lvl-inst/zero))).

dup_ind_def_mk/positivity : ctor-positive "dup_ind_def" lnil (econst "dup_ind_def" lnil)
   = (ctor-positive/intro ([S] S) (ctor-spine/result applies-self/refl)).

dup_ind_def_mk/cmem : cmem "dup_ind_def.mk" (ccons "dup_ind_def.mk" cnil)
   = cmem/here.

dup_ind_def_mk/fuo : field-universes-ok-skip-params liz (econst "dup_ind_def" lnil) (lsucc lzero)
   = (field-universes-ok-skip-params/start
       (field-universes-ok/done not-forall/const)).

dup_ind_def_mk/name : name "dup_ind_def.mk" (is-decl (econst "dup_ind_def" lnil) (ctor "dup_ind_def" liz)).

dup_ind_def_mk/decl : declared "dup_ind_def.mk" (econst "dup_ind_def" lnil) (ctor "dup_ind_def" liz)
   = declared/ok-ctor dup_ind_def_mk/name dup_ind_def/name dup_ind_def_mk/cmem dup_ind_def/eisl dup_ind_def_mk/fuo (dkind-ok/ctor dup_ind_def_mk/type-wf dup_ind_def_mk/positivity).

%% recursor dup_ind_def.rec
dup_ind_def_rec/type-wf-sort : lvl
   = (limax
       (limax (lsucc lzero) (lsucc (lvar liz)))
       (limax (lvar liz) (limax (lsucc lzero) (lvar liz)))).

dup_ind_def_rec/type-wf : of
  (eforall (eforall (econst "dup_ind_def" lnil) ([x] (esort (lvar liz)))) ([x] (eforall (eapp x (econst "dup_ind_def.mk" lnil)) ([y] (eforall (econst "dup_ind_def" lnil) ([z] (eapp x z)))))))
  (esort dup_ind_def_rec/type-wf-sort)
   = (defeq/forall
       (defeq/forall
         (defeq/const
           dup_ind_def/decl
           (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
         ([x] ([h] defeq/sort-refl)))
       ([x]
         ([h]
           (defeq/forall
             (defeq/app
               h
               (defeq/const
                 dup_ind_def_mk/decl
                 (inst-expr/const lvls-inst/nil)))
             ([y]
               ([h1]
                 (defeq/forall
                   (defeq/const
                     dup_ind_def/decl
                     (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
                   ([z] ([h2] (defeq/app h h2)))))))))).

dup_ind_def_rec/ert : enum-rec-type (eforall (eforall (econst "dup_ind_def" lnil) ([x] (esort (lvar liz)))) ([x] (eforall (eapp x (econst "dup_ind_def.mk" lnil)) ([y] (eforall (econst "dup_ind_def" lnil) ([z] (eapp x z))))))) "dup_ind_def" lnil (ccons "dup_ind_def.mk" cnil) (lvar liz)
   = (enum-rec-type/intro
       ([x] ([h] (enum-rec-body/minor ([y] ([h1] enum-rec-body/done)))))).

dup_ind_def_rec/lee : le-eligible (lsucc lzero) (ccons "dup_ind_def.mk" cnil) (lvar liz)
   = le-eligible/csingle.

dup_ind_def_rec/decl : declared "dup_ind_def.rec"
  (eforall (eforall (econst "dup_ind_def" lnil) ([x] (esort (lvar liz)))) ([x] (eforall (eapp x (econst "dup_ind_def.mk" lnil)) ([y] (eforall (econst "dup_ind_def" lnil) ([z] (eapp x z)))))))
  (irec "dup_ind_def")
   = declared/ok-irec dup_ind_def/rec-name (dkind-ok/irec dup_ind_def_rec/type-wf dup_ind_def/name dup_ind_def/eisl dup_ind_def_rec/ert dup_ind_def_rec/lee).
