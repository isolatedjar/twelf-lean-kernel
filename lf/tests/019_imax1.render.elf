%%% Render-only view of: 019_imax1
%%% Type inference for forall using imax
%% def imax1
imax1/render-type : expr =
   (eforall (esort lzero) ([x] (esort lzero))).
imax1/render-value : expr =
   (elam (esort lzero) ([x] (eforall (esort (lsucc lzero)) ([y] x)))).

