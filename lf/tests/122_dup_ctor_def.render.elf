%%% Render-only view of: 122_dup_ctor_def
%%% A definition and a constructor with the same name
%% def dup_ctor_def.mk
%%% HOLE
dup_ctor_def_mk/type-wf : defeq (esort (lsucc lzero)) (esort (lsucc lzero)) (esort lzero).

%%% HOLE
dup_ctor_def_mk/value-typed : defeq (esort lzero) (esort lzero) (esort (lsucc lzero)).

dup_ctor_def_mk/decl : declared "dup_ctor_def.mk" lnil
   (esort (lsucc lzero))
   (defn (esort lzero))
   (dkind-ok/defn dup_ctor_def_mk/type-wf dup_ctor_def_mk/value-typed).

%% inductive dup_ctor_def
%%% HOLE
dup_ctor_def/type-wf : defeq (esort (lsucc lzero)) (esort (lsucc lzero)) (esort lzero).

%%% HOLE
dup_ctor_def/ends-in-sort : ends-in-sort (esort (lsucc lzero)).

dup_ctor_def/decl : declared "dup_ctor_def" lnil
   (esort (lsucc lzero))
   indt
   (dkind-ok/indt dup_ctor_def/type-wf dup_ctor_def/ends-in-sort).

%% ctor dup_ctor_def.mk (of dup_ctor_def)
%%% HOLE
dup_ctor_def_mk/type-wf : defeq (econst "dup_ctor_def" lnil) (econst "dup_ctor_def" lnil) (esort lzero).

%%% HOLE
dup_ctor_def_mk/positivity : ctor-positive "dup_ctor_def" lnil (econst "dup_ctor_def" lnil).

dup_ctor_def_mk/decl : declared "dup_ctor_def.mk" lnil
   (econst "dup_ctor_def" lnil)
   ctor
   (dkind-ok/ctor dup_ctor_def_mk/type-wf dup_ctor_def_mk/positivity).

%% recursor dup_ctor_def.rec
%%% HOLE
dup_ctor_def_rec/type-wf : {u : lvl} defeq (eforall (eforall (econst "dup_ctor_def" lnil) ([x] (esort u))) ([x] (eforall (eapp x (econst "dup_ctor_def.mk" lnil)) ([y] (eforall (econst "dup_ctor_def" lnil) ([z] (eapp x z))))))) (eforall (eforall (econst "dup_ctor_def" lnil) ([x] (esort u))) ([x] (eforall (eapp x (econst "dup_ctor_def.mk" lnil)) ([y] (eforall (econst "dup_ctor_def" lnil) ([z] (eapp x z))))))) (esort lzero).

dup_ctor_def_rec/decl : {u : lvl} declared "dup_ctor_def.rec" (lcons u lnil)
   (eforall (eforall (econst "dup_ctor_def" lnil) ([x] (esort u))) ([x] (eforall (eapp x (econst "dup_ctor_def.mk" lnil)) ([y] (eforall (econst "dup_ctor_def" lnil) ([z] (eapp x z)))))))
   irec
   (dkind-ok/irec (dup_ctor_def_rec/type-wf u)).

