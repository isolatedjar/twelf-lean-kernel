%%% Expected outcome: accept
%%% Asserting the type of the generated recursor
%solve nonneg_0 : 0 >= 0.

sneq/0 : string-neq "Bool.false" "Bool.true".
sneq/1 : string-neq "Bool" "TwoBool".

%% inductive Bool
Bool/type-wf-sort : lvl
   = (lsucc (lsucc lzero)).

Bool/type-wf : of (esort (lsucc lzero)) (esort Bool/type-wf-sort)
   = defeq/sort-refl.

Bool/ends-in-sort : ends-in-sort (esort (lsucc lzero))
   = ends-in-sort/sort.

Bool/rec-name : name "Bool.rec" (is-rec-for "Bool").

Bool/eisl : ends-in-sort-with-level (esort (lsucc lzero)) (lsucc lzero)
   = ends-in-sort-with-level/sort.

Bool/clf : count-leading-foralls (esort (lsucc lzero)) liz
   = count-leading-foralls/zero.

Bool/cnd : cnames-distinct (ccons "Bool.false" (ccons "Bool.true" cnil))
   = (cnames-distinct/cons
       (cnames-distinct/cons cnames-distinct/nil all-string-neq/nil)
       (all-string-neq/cons sneq/0 all-string-neq/nil)).

Bool/name : name "Bool" (is-decl (esort (lsucc lzero)) (indt (ccons "Bool.false" (ccons "Bool.true" cnil)) liz)).

Bool/decl : declared "Bool" (esort (lsucc lzero)) (indt (ccons "Bool.false" (ccons "Bool.true" cnil)) liz)
   = declared/ok-indt Bool/name Bool/rec-name Bool/clf Bool/cnd (dkind-ok/indt Bool/type-wf Bool/ends-in-sort).

%% ctor Bool.false (of Bool)
Bool_false/type-wf-sort : lvl
   = (lsucc lzero).

Bool_false/type-wf : of (econst "Bool" lnil) (esort Bool_false/type-wf-sort)
   = (defeq/const Bool/decl (inst-expr/sort (lvl-inst/succ lvl-inst/zero))).

Bool_false/positivity : ctor-positive "Bool" lnil (econst "Bool" lnil)
   = (ctor-positive/intro ([S] S) (ctor-spine/result applies-self/refl)).

Bool_false/cmem : cmem "Bool.false" (ccons "Bool.false" (ccons "Bool.true" cnil))
   = cmem/here.

Bool_false/fuo : field-universes-ok-skip-params liz (econst "Bool" lnil) (lsucc lzero)
   = (field-universes-ok-skip-params/start
       (field-universes-ok/done not-forall/const)).

Bool_false/name : name "Bool.false" (is-decl (econst "Bool" lnil) (ctor "Bool" liz)).

Bool_false/decl : declared "Bool.false" (econst "Bool" lnil) (ctor "Bool" liz)
   = declared/ok-ctor Bool_false/name Bool/name Bool_false/cmem Bool/eisl Bool_false/fuo (dkind-ok/ctor Bool_false/type-wf Bool_false/positivity).

%% ctor Bool.true (of Bool)
Bool_true/type-wf-sort : lvl
   = (lsucc lzero).

Bool_true/type-wf : of (econst "Bool" lnil) (esort Bool_true/type-wf-sort)
   = (defeq/const Bool/decl (inst-expr/sort (lvl-inst/succ lvl-inst/zero))).

Bool_true/positivity : ctor-positive "Bool" lnil (econst "Bool" lnil)
   = (ctor-positive/intro ([S] S) (ctor-spine/result applies-self/refl)).

Bool_true/cmem : cmem "Bool.true" (ccons "Bool.false" (ccons "Bool.true" cnil))
   = (cmem/there cmem/here).

Bool_true/fuo : field-universes-ok-skip-params liz (econst "Bool" lnil) (lsucc lzero)
   = (field-universes-ok-skip-params/start
       (field-universes-ok/done not-forall/const)).

Bool_true/name : name "Bool.true" (is-decl (econst "Bool" lnil) (ctor "Bool" (lis liz))).

Bool_true/decl : declared "Bool.true" (econst "Bool" lnil) (ctor "Bool" (lis liz))
   = declared/ok-ctor Bool_true/name Bool/name Bool_true/cmem Bool/eisl Bool_true/fuo (dkind-ok/ctor Bool_true/type-wf Bool_true/positivity).

%% recursor Bool.rec
Bool_rec/type-wf-sort : lvl
   = (limax
       (limax (lsucc lzero) (lsucc (lvar liz)))
       (limax (lvar liz) (limax (lvar liz) (limax (lsucc lzero) (lvar liz))))).

Bool_rec/type-wf : of
  (eforall (eforall (econst "Bool" lnil) ([x] (esort (lvar liz))))
    ([x] (eforall (eapp x (econst "Bool.false" lnil)) ([y] (eforall (eapp x (econst "Bool.true" lnil)) ([z] (eforall (econst "Bool" lnil) ([w] (eapp x w)))))))))
  (esort Bool_rec/type-wf-sort)
   = (defeq/forall
       (defeq/forall
         (defeq/const Bool/decl (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
         ([x] ([h] defeq/sort-refl)))
       ([x]
         ([h]
           (defeq/forall
             (defeq/app
               h
               (defeq/const Bool_false/decl (inst-expr/const lvls-inst/nil)))
             ([y]
               ([h1]
                 (defeq/forall
                   (defeq/app
                     h
                     (defeq/const
                       Bool_true/decl
                       (inst-expr/const lvls-inst/nil)))
                   ([z]
                     ([h2]
                       (defeq/forall
                         (defeq/const
                           Bool/decl
                           (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
                         ([w] ([h3] (defeq/app h h3))))))))))))).

Bool_rec/ert : enum-rec-type (eforall (eforall (econst "Bool" lnil) ([x] (esort (lvar liz)))) ([x] (eforall (eapp x (econst "Bool.false" lnil)) ([y] (eforall (eapp x (econst "Bool.true" lnil)) ([z] (eforall (econst "Bool" lnil) ([w] (eapp x w))))))))) "Bool" lnil (ccons "Bool.false" (ccons "Bool.true" cnil)) (lvar liz)
   = (enum-rec-type/intro
       ([x]
         ([h]
           (enum-rec-body/minor
             ([y]
               ([h1] (enum-rec-body/minor ([z] ([h2] enum-rec-body/done))))))))).

Bool_rec/lee : le-eligible (lsucc lzero) (ccons "Bool.false" (ccons "Bool.true" cnil)) (lvar liz)
   = (le-eligible/non-prop (mleq/self nonneg_0)).

Bool_rec/decl : declared "Bool.rec"
  (eforall (eforall (econst "Bool" lnil) ([x] (esort (lvar liz))))
    ([x] (eforall (eapp x (econst "Bool.false" lnil)) ([y] (eforall (eapp x (econst "Bool.true" lnil)) ([z] (eforall (econst "Bool" lnil) ([w] (eapp x w)))))))))
  (irec "Bool")
   = declared/ok-irec Bool/rec-name (dkind-ok/irec Bool_rec/type-wf Bool/name Bool/eisl Bool_rec/ert Bool_rec/lee).

%% inductive TwoBool
TwoBool/type-wf-sort : lvl
   = (lsucc (lsucc lzero)).

TwoBool/type-wf : of (esort (lsucc lzero)) (esort TwoBool/type-wf-sort)
   = defeq/sort-refl.

TwoBool/ends-in-sort : ends-in-sort (esort (lsucc lzero))
   = ends-in-sort/sort.

TwoBool/rec-name : name "TwoBool.rec" (is-rec-for "TwoBool").

TwoBool/eisl : ends-in-sort-with-level (esort (lsucc lzero)) (lsucc lzero)
   = ends-in-sort-with-level/sort.

TwoBool/clf : count-leading-foralls (esort (lsucc lzero)) liz
   = count-leading-foralls/zero.

TwoBool/cnd : cnames-distinct (ccons "TwoBool.mk" cnil)
   = (cnames-distinct/cons cnames-distinct/nil all-string-neq/nil).

TwoBool/name : name "TwoBool" (is-decl (esort (lsucc lzero)) (indt (ccons "TwoBool.mk" cnil) liz)).

TwoBool/decl : declared "TwoBool" (esort (lsucc lzero)) (indt (ccons "TwoBool.mk" cnil) liz)
   = declared/ok-indt TwoBool/name TwoBool/rec-name TwoBool/clf TwoBool/cnd (dkind-ok/indt TwoBool/type-wf TwoBool/ends-in-sort).

%% ctor TwoBool.mk (of TwoBool)
TwoBool_mk/type-wf-sort : lvl
   = (limax (lsucc lzero) (limax (lsucc lzero) (lsucc lzero))).

TwoBool_mk/type-wf : of (eforall (econst "Bool" lnil) ([x] (eforall (econst "Bool" lnil) ([y] (econst "TwoBool" lnil))))) (esort TwoBool_mk/type-wf-sort)
   = (defeq/forall
       (defeq/const Bool/decl (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
       ([x]
         ([h]
           (defeq/forall
             (defeq/const
               Bool/decl
               (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
             ([y]
               ([h1]
                 (defeq/const
                   TwoBool/decl
                   (inst-expr/sort (lvl-inst/succ lvl-inst/zero))))))))).

TwoBool_mk/positivity : ctor-positive "TwoBool" lnil (eforall (econst "Bool" lnil) ([x] (eforall (econst "Bool" lnil) ([y] (econst "TwoBool" lnil)))))
   = (ctor-positive/intro ([S] (eforall (econst "Bool" lnil) ([x] (eforall (econst "Bool" lnil) ([y] S))))) (ctor-spine/arg
       (strict-pos/no-occur (econst "Bool" lnil) (no-self-ref/const sneq/1))
       ([x]
         ([y]
           (ctor-spine/arg
             (strict-pos/no-occur
               (econst "Bool" lnil)
               (no-self-ref/const sneq/1))
             ([z] ([w] (ctor-spine/result applies-self/refl)))))))).

TwoBool_mk/cmem : cmem "TwoBool.mk" (ccons "TwoBool.mk" cnil)
   = cmem/here.

TwoBool_mk/fuo : field-universes-ok-skip-params liz (eforall (econst "Bool" lnil) ([x] (eforall (econst "Bool" lnil) ([y] (econst "TwoBool" lnil))))) (lsucc lzero)
   = (field-universes-ok-skip-params/start
       (field-universes-ok/forall
         (defeq/const Bool/decl (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
         (mleq/self nonneg_0)
         ([x]
           ([h]
             (field-universes-ok/forall
               (defeq/const
                 Bool/decl
                 (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
               (mleq/self nonneg_0)
               ([y] ([h1] (field-universes-ok/done not-forall/const)))))))).

TwoBool_mk/name : name "TwoBool.mk" (is-decl (eforall (econst "Bool" lnil) ([x] (eforall (econst "Bool" lnil) ([y] (econst "TwoBool" lnil))))) (ctor "TwoBool" liz)).

TwoBool_mk/decl : declared "TwoBool.mk" (eforall (econst "Bool" lnil) ([x] (eforall (econst "Bool" lnil) ([y] (econst "TwoBool" lnil))))) (ctor "TwoBool" liz)
   = declared/ok-ctor TwoBool_mk/name TwoBool/name TwoBool_mk/cmem TwoBool/eisl TwoBool_mk/fuo (dkind-ok/ctor TwoBool_mk/type-wf TwoBool_mk/positivity).

%% recursor TwoBool.rec
TwoBool_rec/type-wf-sort : lvl
   = (limax
       (limax (lsucc lzero) (lsucc (lvar liz)))
       (limax
         (limax (lsucc lzero) (limax (lsucc lzero) (lvar liz)))
         (limax (lsucc lzero) (lvar liz)))).

TwoBool_rec/type-wf : of
  (eforall (eforall (econst "TwoBool" lnil) ([x] (esort (lvar liz))))
    ([x]
      (eforall (eforall (econst "Bool" lnil) ([y] (eforall (econst "Bool" lnil) ([z] (eapp x (eapp (eapp (econst "TwoBool.mk" lnil) y) z))))))
        ([y] (eforall (econst "TwoBool" lnil) ([z] (eapp x z)))))))
  (esort TwoBool_rec/type-wf-sort)
   = (defeq/forall
       (defeq/forall
         (defeq/const
           TwoBool/decl
           (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
         ([x] ([h] defeq/sort-refl)))
       ([x]
         ([h]
           (defeq/forall
             (defeq/forall
               (defeq/const
                 Bool/decl
                 (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
               ([y]
                 ([h1]
                   (defeq/forall
                     (defeq/const
                       Bool/decl
                       (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
                     ([z]
                       ([h2]
                         (defeq/app
                           h
                           (defeq/app
                             (defeq/app
                               (defeq/const
                                 TwoBool_mk/decl
                                 (inst-expr/forall
                                   (inst-expr/const lvls-inst/nil)
                                   ([x]
                                     ([h]
                                       (inst-expr/forall
                                         (inst-expr/const lvls-inst/nil)
                                         ([y]
                                           ([h1]
                                             (inst-expr/const
                                               lvls-inst/nil))))))))
                               h1)
                             h2))))))))
             ([y]
               ([h1]
                 (defeq/forall
                   (defeq/const
                     TwoBool/decl
                     (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
                   ([z] ([h2] (defeq/app h h2)))))))))).

%%% HOLE: recursor TwoBool.rec — non-enum shape (parametric/indexed inductive, or ctor with fields) — §3.1 canonical-schema only handles plain enums in phase 1
TwoBool_rec/decl : declared "TwoBool.rec"
  (eforall (eforall (econst "TwoBool" lnil) ([x] (esort (lvar liz))))
    ([x]
      (eforall (eforall (econst "Bool" lnil) ([y] (eforall (econst "Bool" lnil) ([z] (eapp x (eapp (eapp (econst "TwoBool.mk" lnil) y) z))))))
        ([y] (eforall (econst "TwoBool" lnil) ([z] (eapp x z)))))))
  (irec "TwoBool").

%% def twoBoolRec
twoBoolRec/type-wf-sort : lvl
   = (limax
       (limax (lsucc lzero) (lsucc (lvar liz)))
       (limax
         (limax (lsucc lzero) (limax (lsucc lzero) (lvar liz)))
         (limax (lsucc lzero) (lvar liz)))).

twoBoolRec/type-wf : of
  (eforall (eforall (econst "TwoBool" lnil) ([x] (esort (lvar liz))))
    ([x]
      (eforall (eforall (econst "Bool" lnil) ([y] (eforall (econst "Bool" lnil) ([z] (eapp x (eapp (eapp (econst "TwoBool.mk" lnil) y) z))))))
        ([y] (eforall (econst "TwoBool" lnil) ([z] (eapp x z)))))))
  (esort twoBoolRec/type-wf-sort)
   = (defeq/forall
       (defeq/forall
         (defeq/const
           TwoBool/decl
           (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
         ([x] ([h] defeq/sort-refl)))
       ([x]
         ([h]
           (defeq/forall
             (defeq/forall
               (defeq/const
                 Bool/decl
                 (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
               ([y]
                 ([h1]
                   (defeq/forall
                     (defeq/const
                       Bool/decl
                       (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
                     ([z]
                       ([h2]
                         (defeq/app
                           h
                           (defeq/app
                             (defeq/app
                               (defeq/const
                                 TwoBool_mk/decl
                                 (inst-expr/forall
                                   (inst-expr/const lvls-inst/nil)
                                   ([x]
                                     ([h]
                                       (inst-expr/forall
                                         (inst-expr/const lvls-inst/nil)
                                         ([y]
                                           ([h1]
                                             (inst-expr/const
                                               lvls-inst/nil))))))))
                               h1)
                             h2))))))))
             ([y]
               ([h1]
                 (defeq/forall
                   (defeq/const
                     TwoBool/decl
                     (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
                   ([z] ([h2] (defeq/app h h2)))))))))).

twoBoolRec/value-typed : of
  (elam (eforall (econst "TwoBool" lnil) ([x] (esort (lvar liz))))
    ([x]
      (elam (eforall (econst "Bool" lnil) ([y] (eforall (econst "Bool" lnil) ([z] (eapp x (eapp (eapp (econst "TwoBool.mk" lnil) y) z))))))
        ([y] (elam (econst "TwoBool" lnil) ([z] (eapp (eapp (eapp (econst "TwoBool.rec" (lcons (lvar liz) lnil)) x) y) z)))))))
  (eforall (eforall (econst "TwoBool" lnil) ([x] (esort (lvar liz))))
    ([x]
      (eforall (eforall (econst "Bool" lnil) ([y] (eforall (econst "Bool" lnil) ([z] (eapp x (eapp (eapp (econst "TwoBool.mk" lnil) y) z))))))
        ([y] (eforall (econst "TwoBool" lnil) ([z] (eapp x z)))))))
   = (defeq/lam
       (defeq/forall
         (defeq/const
           TwoBool/decl
           (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
         ([x] ([h] defeq/sort-refl)))
       ([x]
         ([h]
           (defeq/lam
             (defeq/forall
               (defeq/const
                 Bool/decl
                 (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
               ([y]
                 ([h1]
                   (defeq/forall
                     (defeq/const
                       Bool/decl
                       (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
                     ([z]
                       ([h2]
                         (defeq/app
                           h
                           (defeq/app
                             (defeq/app
                               (defeq/const
                                 TwoBool_mk/decl
                                 (inst-expr/forall
                                   (inst-expr/const lvls-inst/nil)
                                   ([x]
                                     ([h]
                                       (inst-expr/forall
                                         (inst-expr/const lvls-inst/nil)
                                         ([y]
                                           ([h1]
                                             (inst-expr/const
                                               lvls-inst/nil))))))))
                               h1)
                             h2))))))))
             ([y]
               ([h1]
                 (defeq/lam
                   (defeq/const
                     TwoBool/decl
                     (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
                   ([z]
                     ([h2]
                       (defeq/app
                         (defeq/app
                           (defeq/app
                             (defeq/const
                               TwoBool_rec/decl
                               (inst-expr/forall
                                 (inst-expr/forall
                                   (inst-expr/const lvls-inst/nil)
                                   ([x]
                                     ([h]
                                       (inst-expr/sort
                                         (lvl-inst/var lvl-nth/zero)))))
                                 ([x]
                                   ([h]
                                     (inst-expr/forall
                                       (inst-expr/forall
                                         (inst-expr/const lvls-inst/nil)
                                         ([y]
                                           ([h1]
                                             (inst-expr/forall
                                               (inst-expr/const lvls-inst/nil)
                                               ([z]
                                                 ([h2]
                                                   (inst-expr/app
                                                     h
                                                     (inst-expr/app
                                                       (inst-expr/app
                                                         (inst-expr/const
                                                           lvls-inst/nil)
                                                         h1)
                                                       h2))))))))
                                       ([y]
                                         ([h1]
                                           (inst-expr/forall
                                             (inst-expr/const lvls-inst/nil)
                                             ([z]
                                               ([h2]
                                                 (inst-expr/app h h2)))))))))))
                             h)
                           h1)
                         h2)))))))))).

twoBoolRec/name : name "twoBoolRec" (is-decl
  (eforall (eforall (econst "TwoBool" lnil) ([x] (esort (lvar liz))))
    ([x]
      (eforall (eforall (econst "Bool" lnil) ([y] (eforall (econst "Bool" lnil) ([z] (eapp x (eapp (eapp (econst "TwoBool.mk" lnil) y) z))))))
        ([y] (eforall (econst "TwoBool" lnil) ([z] (eapp x z)))))))
  (defn (elam (eforall (econst "TwoBool" lnil) ([x] (esort (lvar liz)))) ([x] (elam (eforall (econst "Bool" lnil) ([y] (eforall (econst "Bool" lnil) ([z] (eapp x (eapp (eapp (econst "TwoBool.mk" lnil) y) z)))))) ([y] (elam (econst "TwoBool" lnil) ([z] (eapp (eapp (eapp (econst "TwoBool.rec" (lcons (lvar liz) lnil)) x) y) z))))))))).

twoBoolRec/decl : declared "twoBoolRec"
  (eforall (eforall (econst "TwoBool" lnil) ([x] (esort (lvar liz))))
    ([x]
      (eforall (eforall (econst "Bool" lnil) ([y] (eforall (econst "Bool" lnil) ([z] (eapp x (eapp (eapp (econst "TwoBool.mk" lnil) y) z))))))
        ([y] (eforall (econst "TwoBool" lnil) ([z] (eapp x z)))))))
  (defn (elam (eforall (econst "TwoBool" lnil) ([x] (esort (lvar liz)))) ([x] (elam (eforall (econst "Bool" lnil) ([y] (eforall (econst "Bool" lnil) ([z] (eapp x (eapp (eapp (econst "TwoBool.mk" lnil) y) z)))))) ([y] (elam (econst "TwoBool" lnil) ([z] (eapp (eapp (eapp (econst "TwoBool.rec" (lcons (lvar liz) lnil)) x) y) z))))))))
   = declared/ok twoBoolRec/name dkind-non-inductive/defn (dkind-ok/defn twoBoolRec/type-wf twoBoolRec/value-typed).
