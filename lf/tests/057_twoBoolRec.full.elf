%%% Expected outcome: accept
%%% Asserting the type of the generated recursor
sneq/0 : string-neq "Bool" "TwoBool".

%% inductive Bool
Bool/type-wf-sort : lvl
   = (lsucc (lsucc lzero)).

Bool/type-wf : of (esort (lsucc lzero)) (esort Bool/type-wf-sort)
   = defeq/sort-refl.

Bool/ends-in-sort : ends-in-sort (esort (lsucc lzero))
   = ends-in-sort/sort.

Bool/rec-name : name "Bool.rec" (is-rec-for "Bool").

Bool/name : name "Bool" (is-decl (esort (lsucc lzero)) indt).

Bool/decl : declared "Bool" (esort (lsucc lzero)) indt
   = declared/ok Bool/name (dkind-ok/indt Bool/type-wf Bool/ends-in-sort).

%% ctor Bool.false (of Bool)
Bool_false/type-wf-sort : lvl
   = (lsucc lzero).

Bool_false/type-wf : of (econst "Bool" lnil) (esort Bool_false/type-wf-sort)
   = (defeq/const Bool/decl (inst-expr/sort (lvl-inst/succ lvl-inst/zero))).

Bool_false/positivity : ctor-positive "Bool" lnil (econst "Bool" lnil)
   = (ctor-positive/intro ([S] S) (ctor-spine/result applies-self/refl)).

Bool_false/eisl : ends-in-sort-with-level (esort (lsucc lzero)) (lsucc lzero)
   = ends-in-sort-with-level/sort.

%%% HOLE: Bool_false/fuo — prover for field-universes-ok-skip-params not yet implemented
Bool_false/fuo : field-universes-ok-skip-params liz (econst "Bool" lnil) (lsucc lzero).

Bool_false/name : name "Bool.false" (is-decl (econst "Bool" lnil) ctor).

Bool_false/decl : declared "Bool.false" (econst "Bool" lnil) ctor
   = declared/ok Bool_false/name (dkind-ok/ctor Bool_false/type-wf Bool_false/positivity Bool/decl Bool_false/eisl Bool_false/fuo).

%% ctor Bool.true (of Bool)
Bool_true/type-wf-sort : lvl
   = (lsucc lzero).

Bool_true/type-wf : of (econst "Bool" lnil) (esort Bool_true/type-wf-sort)
   = (defeq/const Bool/decl (inst-expr/sort (lvl-inst/succ lvl-inst/zero))).

Bool_true/positivity : ctor-positive "Bool" lnil (econst "Bool" lnil)
   = (ctor-positive/intro ([S] S) (ctor-spine/result applies-self/refl)).

Bool_true/eisl : ends-in-sort-with-level (esort (lsucc lzero)) (lsucc lzero)
   = ends-in-sort-with-level/sort.

%%% HOLE: Bool_true/fuo — prover for field-universes-ok-skip-params not yet implemented
Bool_true/fuo : field-universes-ok-skip-params liz (econst "Bool" lnil) (lsucc lzero).

Bool_true/name : name "Bool.true" (is-decl (econst "Bool" lnil) ctor).

Bool_true/decl : declared "Bool.true" (econst "Bool" lnil) ctor
   = declared/ok Bool_true/name (dkind-ok/ctor Bool_true/type-wf Bool_true/positivity Bool/decl Bool_true/eisl Bool_true/fuo).

%% recursor Bool.rec
Bool_rec/type-wf-sort : lvl
   = (limax
       (limax (lsucc lzero) (lsucc (lvar liz)))
       (limax (lvar liz) (limax (lvar liz) (limax (lsucc lzero) (lvar liz))))).

Bool_rec/type-wf : of
  (eforall (eforall (econst "Bool" lnil) ([x] (esort (lvar liz))))
    ([x] (eforall (eapp x (econst "Bool.false" lnil)) ([y] (eforall (eapp x (econst "Bool.true" lnil)) ([z] (eforall (econst "Bool" lnil) ([w] (eapp x w)))))))))
  (esort Bool_rec/type-wf-sort)
   = (defeq/forall
       (defeq/forall
         (defeq/const Bool/decl (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
         ([x] ([h] defeq/sort-refl)))
       ([x]
         ([h]
           (defeq/forall
             (defeq/app
               h
               (defeq/const Bool_false/decl (inst-expr/const lvls-inst/nil)))
             ([y]
               ([h1]
                 (defeq/forall
                   (defeq/app
                     h
                     (defeq/const
                       Bool_true/decl
                       (inst-expr/const lvls-inst/nil)))
                   ([z]
                     ([h2]
                       (defeq/forall
                         (defeq/const
                           Bool/decl
                           (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
                         ([w] ([h3] (defeq/app h h3))))))))))))).

Bool_rec/decl : declared "Bool.rec"
  (eforall (eforall (econst "Bool" lnil) ([x] (esort (lvar liz))))
    ([x] (eforall (eapp x (econst "Bool.false" lnil)) ([y] (eforall (eapp x (econst "Bool.true" lnil)) ([z] (eforall (econst "Bool" lnil) ([w] (eapp x w)))))))))
  irec
   = declared/ok-irec Bool/rec-name (dkind-ok/irec Bool_rec/type-wf).

%% inductive TwoBool
TwoBool/type-wf-sort : lvl
   = (lsucc (lsucc lzero)).

TwoBool/type-wf : of (esort (lsucc lzero)) (esort TwoBool/type-wf-sort)
   = defeq/sort-refl.

TwoBool/ends-in-sort : ends-in-sort (esort (lsucc lzero))
   = ends-in-sort/sort.

TwoBool/rec-name : name "TwoBool.rec" (is-rec-for "TwoBool").

TwoBool/name : name "TwoBool" (is-decl (esort (lsucc lzero)) indt).

TwoBool/decl : declared "TwoBool" (esort (lsucc lzero)) indt
   = declared/ok TwoBool/name (dkind-ok/indt TwoBool/type-wf TwoBool/ends-in-sort).

%% ctor TwoBool.mk (of TwoBool)
TwoBool_mk/type-wf-sort : lvl
   = (limax (lsucc lzero) (limax (lsucc lzero) (lsucc lzero))).

TwoBool_mk/type-wf : of (eforall (econst "Bool" lnil) ([x] (eforall (econst "Bool" lnil) ([y] (econst "TwoBool" lnil))))) (esort TwoBool_mk/type-wf-sort)
   = (defeq/forall
       (defeq/const Bool/decl (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
       ([x]
         ([h]
           (defeq/forall
             (defeq/const
               Bool/decl
               (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
             ([y]
               ([h1]
                 (defeq/const
                   TwoBool/decl
                   (inst-expr/sort (lvl-inst/succ lvl-inst/zero))))))))).

TwoBool_mk/positivity : ctor-positive "TwoBool" lnil (eforall (econst "Bool" lnil) ([x] (eforall (econst "Bool" lnil) ([y] (econst "TwoBool" lnil)))))
   = (ctor-positive/intro ([S] (eforall (econst "Bool" lnil) ([x] (eforall (econst "Bool" lnil) ([y] S))))) (ctor-spine/arg
       (strict-pos/no-occur (econst "Bool" lnil) (no-self-ref/const sneq/0))
       ([x]
         ([y]
           (ctor-spine/arg
             (strict-pos/no-occur
               (econst "Bool" lnil)
               (no-self-ref/const sneq/0))
             ([z] ([w] (ctor-spine/result applies-self/refl)))))))).

TwoBool_mk/eisl : ends-in-sort-with-level (esort (lsucc lzero)) (lsucc lzero)
   = ends-in-sort-with-level/sort.

%%% HOLE: TwoBool_mk/fuo — prover for field-universes-ok-skip-params not yet implemented
TwoBool_mk/fuo : field-universes-ok-skip-params liz (eforall (econst "Bool" lnil) ([x] (eforall (econst "Bool" lnil) ([y] (econst "TwoBool" lnil))))) (lsucc lzero).

TwoBool_mk/name : name "TwoBool.mk" (is-decl (eforall (econst "Bool" lnil) ([x] (eforall (econst "Bool" lnil) ([y] (econst "TwoBool" lnil))))) ctor).

TwoBool_mk/decl : declared "TwoBool.mk" (eforall (econst "Bool" lnil) ([x] (eforall (econst "Bool" lnil) ([y] (econst "TwoBool" lnil))))) ctor
   = declared/ok TwoBool_mk/name (dkind-ok/ctor TwoBool_mk/type-wf TwoBool_mk/positivity TwoBool/decl TwoBool_mk/eisl TwoBool_mk/fuo).

%% recursor TwoBool.rec
TwoBool_rec/type-wf-sort : lvl
   = (limax
       (limax (lsucc lzero) (lsucc (lvar liz)))
       (limax
         (limax (lsucc lzero) (limax (lsucc lzero) (lvar liz)))
         (limax (lsucc lzero) (lvar liz)))).

TwoBool_rec/type-wf : of
  (eforall (eforall (econst "TwoBool" lnil) ([x] (esort (lvar liz))))
    ([x]
      (eforall (eforall (econst "Bool" lnil) ([y] (eforall (econst "Bool" lnil) ([z] (eapp x (eapp (eapp (econst "TwoBool.mk" lnil) y) z))))))
        ([y] (eforall (econst "TwoBool" lnil) ([z] (eapp x z)))))))
  (esort TwoBool_rec/type-wf-sort)
   = (defeq/forall
       (defeq/forall
         (defeq/const
           TwoBool/decl
           (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
         ([x] ([h] defeq/sort-refl)))
       ([x]
         ([h]
           (defeq/forall
             (defeq/forall
               (defeq/const
                 Bool/decl
                 (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
               ([y]
                 ([h1]
                   (defeq/forall
                     (defeq/const
                       Bool/decl
                       (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
                     ([z]
                       ([h2]
                         (defeq/app
                           h
                           (defeq/app
                             (defeq/app
                               (defeq/const
                                 TwoBool_mk/decl
                                 (inst-expr/forall
                                   (inst-expr/const lvls-inst/nil)
                                   ([x]
                                     ([h]
                                       (inst-expr/forall
                                         (inst-expr/const lvls-inst/nil)
                                         ([y]
                                           ([h1]
                                             (inst-expr/const
                                               lvls-inst/nil))))))))
                               h1)
                             h2))))))))
             ([y]
               ([h1]
                 (defeq/forall
                   (defeq/const
                     TwoBool/decl
                     (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
                   ([z] ([h2] (defeq/app h h2)))))))))).

TwoBool_rec/decl : declared "TwoBool.rec"
  (eforall (eforall (econst "TwoBool" lnil) ([x] (esort (lvar liz))))
    ([x]
      (eforall (eforall (econst "Bool" lnil) ([y] (eforall (econst "Bool" lnil) ([z] (eapp x (eapp (eapp (econst "TwoBool.mk" lnil) y) z))))))
        ([y] (eforall (econst "TwoBool" lnil) ([z] (eapp x z)))))))
  irec
   = declared/ok-irec TwoBool/rec-name (dkind-ok/irec TwoBool_rec/type-wf).

%% def twoBoolRec
twoBoolRec/type-wf-sort : lvl
   = (limax
       (limax (lsucc lzero) (lsucc (lvar liz)))
       (limax
         (limax (lsucc lzero) (limax (lsucc lzero) (lvar liz)))
         (limax (lsucc lzero) (lvar liz)))).

twoBoolRec/type-wf : of
  (eforall (eforall (econst "TwoBool" lnil) ([x] (esort (lvar liz))))
    ([x]
      (eforall (eforall (econst "Bool" lnil) ([y] (eforall (econst "Bool" lnil) ([z] (eapp x (eapp (eapp (econst "TwoBool.mk" lnil) y) z))))))
        ([y] (eforall (econst "TwoBool" lnil) ([z] (eapp x z)))))))
  (esort twoBoolRec/type-wf-sort)
   = (defeq/forall
       (defeq/forall
         (defeq/const
           TwoBool/decl
           (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
         ([x] ([h] defeq/sort-refl)))
       ([x]
         ([h]
           (defeq/forall
             (defeq/forall
               (defeq/const
                 Bool/decl
                 (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
               ([y]
                 ([h1]
                   (defeq/forall
                     (defeq/const
                       Bool/decl
                       (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
                     ([z]
                       ([h2]
                         (defeq/app
                           h
                           (defeq/app
                             (defeq/app
                               (defeq/const
                                 TwoBool_mk/decl
                                 (inst-expr/forall
                                   (inst-expr/const lvls-inst/nil)
                                   ([x]
                                     ([h]
                                       (inst-expr/forall
                                         (inst-expr/const lvls-inst/nil)
                                         ([y]
                                           ([h1]
                                             (inst-expr/const
                                               lvls-inst/nil))))))))
                               h1)
                             h2))))))))
             ([y]
               ([h1]
                 (defeq/forall
                   (defeq/const
                     TwoBool/decl
                     (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
                   ([z] ([h2] (defeq/app h h2)))))))))).

twoBoolRec/value-typed : of
  (elam (eforall (econst "TwoBool" lnil) ([x] (esort (lvar liz))))
    ([x]
      (elam (eforall (econst "Bool" lnil) ([y] (eforall (econst "Bool" lnil) ([z] (eapp x (eapp (eapp (econst "TwoBool.mk" lnil) y) z))))))
        ([y] (elam (econst "TwoBool" lnil) ([z] (eapp (eapp (eapp (econst "TwoBool.rec" (lcons (lvar liz) lnil)) x) y) z)))))))
  (eforall (eforall (econst "TwoBool" lnil) ([x] (esort (lvar liz))))
    ([x]
      (eforall (eforall (econst "Bool" lnil) ([y] (eforall (econst "Bool" lnil) ([z] (eapp x (eapp (eapp (econst "TwoBool.mk" lnil) y) z))))))
        ([y] (eforall (econst "TwoBool" lnil) ([z] (eapp x z)))))))
   = (defeq/lam
       (defeq/forall
         (defeq/const
           TwoBool/decl
           (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
         ([x] ([h] defeq/sort-refl)))
       ([x]
         ([h]
           (defeq/lam
             (defeq/forall
               (defeq/const
                 Bool/decl
                 (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
               ([y]
                 ([h1]
                   (defeq/forall
                     (defeq/const
                       Bool/decl
                       (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
                     ([z]
                       ([h2]
                         (defeq/app
                           h
                           (defeq/app
                             (defeq/app
                               (defeq/const
                                 TwoBool_mk/decl
                                 (inst-expr/forall
                                   (inst-expr/const lvls-inst/nil)
                                   ([x]
                                     ([h]
                                       (inst-expr/forall
                                         (inst-expr/const lvls-inst/nil)
                                         ([y]
                                           ([h1]
                                             (inst-expr/const
                                               lvls-inst/nil))))))))
                               h1)
                             h2))))))))
             ([y]
               ([h1]
                 (defeq/lam
                   (defeq/const
                     TwoBool/decl
                     (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
                   ([z]
                     ([h2]
                       (defeq/app
                         (defeq/app
                           (defeq/app
                             (defeq/const
                               TwoBool_rec/decl
                               (inst-expr/forall
                                 (inst-expr/forall
                                   (inst-expr/const lvls-inst/nil)
                                   ([x]
                                     ([h]
                                       (inst-expr/sort
                                         (lvl-inst/var lvl-nth/zero)))))
                                 ([x]
                                   ([h]
                                     (inst-expr/forall
                                       (inst-expr/forall
                                         (inst-expr/const lvls-inst/nil)
                                         ([y]
                                           ([h1]
                                             (inst-expr/forall
                                               (inst-expr/const lvls-inst/nil)
                                               ([z]
                                                 ([h2]
                                                   (inst-expr/app
                                                     h
                                                     (inst-expr/app
                                                       (inst-expr/app
                                                         (inst-expr/const
                                                           lvls-inst/nil)
                                                         h1)
                                                       h2))))))))
                                       ([y]
                                         ([h1]
                                           (inst-expr/forall
                                             (inst-expr/const lvls-inst/nil)
                                             ([z]
                                               ([h2]
                                                 (inst-expr/app h h2)))))))))))
                             h)
                           h1)
                         h2)))))))))).

twoBoolRec/name : name "twoBoolRec" (is-decl
  (eforall (eforall (econst "TwoBool" lnil) ([x] (esort (lvar liz))))
    ([x]
      (eforall (eforall (econst "Bool" lnil) ([y] (eforall (econst "Bool" lnil) ([z] (eapp x (eapp (eapp (econst "TwoBool.mk" lnil) y) z))))))
        ([y] (eforall (econst "TwoBool" lnil) ([z] (eapp x z)))))))
  (defn (elam (eforall (econst "TwoBool" lnil) ([x] (esort (lvar liz)))) ([x] (elam (eforall (econst "Bool" lnil) ([y] (eforall (econst "Bool" lnil) ([z] (eapp x (eapp (eapp (econst "TwoBool.mk" lnil) y) z)))))) ([y] (elam (econst "TwoBool" lnil) ([z] (eapp (eapp (eapp (econst "TwoBool.rec" (lcons (lvar liz) lnil)) x) y) z))))))))).

twoBoolRec/decl : declared "twoBoolRec"
  (eforall (eforall (econst "TwoBool" lnil) ([x] (esort (lvar liz))))
    ([x]
      (eforall (eforall (econst "Bool" lnil) ([y] (eforall (econst "Bool" lnil) ([z] (eapp x (eapp (eapp (econst "TwoBool.mk" lnil) y) z))))))
        ([y] (eforall (econst "TwoBool" lnil) ([z] (eapp x z)))))))
  (defn (elam (eforall (econst "TwoBool" lnil) ([x] (esort (lvar liz)))) ([x] (elam (eforall (econst "Bool" lnil) ([y] (eforall (econst "Bool" lnil) ([z] (eapp x (eapp (eapp (econst "TwoBool.mk" lnil) y) z)))))) ([y] (elam (econst "TwoBool" lnil) ([z] (eapp (eapp (eapp (econst "TwoBool.rec" (lcons (lvar liz) lnil)) x) y) z))))))))
   = declared/ok twoBoolRec/name (dkind-ok/defn twoBoolRec/type-wf twoBoolRec/value-typed).
