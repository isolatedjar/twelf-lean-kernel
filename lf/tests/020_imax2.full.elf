%%% Expected outcome: accept
%%% Type inference for forall using imax
%% def imax2
imax2/type-wf : of (eforall (esort (lsucc lzero)) ([x] (esort (lsucc (lsucc lzero))))) (esort (limax (lsucc (lsucc lzero)) (lsucc (lsucc (lsucc lzero)))))
   = (defeq/forall defeq/sort-refl ([x] ([h] defeq/sort-refl))).

imax2/value-typed : of (elam (esort (lsucc lzero)) ([x] (eforall (esort (lsucc lzero)) ([y] x)))) (eforall (esort (lsucc lzero)) ([x] (esort (lsucc (lsucc lzero)))))
   = (defeq/conv
       (defeq/forall
         (defeq/sort-eq eq-lvl/refl)
         ([x]
           ([h]
             (defeq/sort-eq
               (eq-lvl/le
                 (leq-lvl/imaxRs (leq-lvl/maxR1 leq-lvl/self))
                 (leq-lvl/imaxLs
                   (leq-lvl/sR
                     (leq-lvl/sR
                       (leq-lvl/maxL
                         (leq-lvl/sL leq-lvl/self)
                         (leq-lvl/sL (leq-lvl/sL leq-lvl/self)))))))))))
       (defeq/lam
         defeq/sort-refl
         ([x] ([h] (defeq/forall defeq/sort-refl ([y] ([h1] h))))))).

imax2/name : name "imax2" (is-decl (eforall (esort (lsucc lzero)) ([x] (esort (lsucc (lsucc lzero))))) (defn (elam (esort (lsucc lzero)) ([x] (eforall (esort (lsucc lzero)) ([y] x)))))).

imax2/decl : declared "imax2" (eforall (esort (lsucc lzero)) ([x] (esort (lsucc (lsucc lzero))))) (defn (elam (esort (lsucc lzero)) ([x] (eforall (esort (lsucc lzero)) ([y] x)))))
   = declared/ok imax2/name dkind-non-inductive/defn (dkind-ok/defn imax2/type-wf imax2/value-typed).
