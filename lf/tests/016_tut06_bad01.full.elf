%%% Expected outcome: reject
%%% Duplicate universe parameters
%% def tut06_bad01
tut06_bad01/type-wf-sort : lvl
   = (lsucc (lsucc lzero)).

tut06_bad01/type-wf : defeq (esort (lsucc lzero)) (esort (lsucc lzero)) (esort tut06_bad01/type-wf-sort)
   = defeq/sort-refl.

tut06_bad01/value-typed : defeq (esort lzero) (esort lzero) (esort (lsucc lzero))
   = defeq/sort-refl.

tut06_bad01/name : name "tut06_bad01" (is-decl (esort (lsucc lzero)) (defn (esort lzero))).

tut06_bad01/decl : declared "tut06_bad01" (esort (lsucc lzero)) (defn (esort lzero))
   = declared/ok tut06_bad01/name (dkind-ok/defn tut06_bad01/type-wf tut06_bad01/value-typed).

