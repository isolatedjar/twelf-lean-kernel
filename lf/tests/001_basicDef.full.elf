%%% Expected outcome: accept
%%% Basic definition
%% def basicDef
basicDef/type-wf : of (esort (lsucc lzero)) (esort (lsucc (lsucc lzero)))
   = defeq/sort-refl.

basicDef/value-typed : of (esort lzero) (esort (lsucc lzero))
   = defeq/sort-refl.

basicDef/name : name "basicDef" (is-decl (esort (lsucc lzero)) (defn (esort lzero))).

basicDef/decl : declared "basicDef" (esort (lsucc lzero)) (defn (esort lzero))
   = declared/ok basicDef/name dkind-non-inductive/defn (dkind-ok/defn basicDef/type-wf basicDef/value-typed).
