%%% Render-only view of: 026_letType
%%% Type checking a non-dependent let
%% def letType
letType/render-type : expr =
   (esort (lsucc lzero)).
letType/render-value : expr =
   (eapp (elam (esort (lsucc lzero)) ([x] x)) (esort lzero)).

