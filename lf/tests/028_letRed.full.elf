%%% Expected outcome: accept
%%% Reducing a let
%% axiom aProp
%%% HOLE
aProp/type-wf : defeq (esort lzero) (esort lzero) (esort lzero).

aProp/decl : declared "aProp" lnil
   (esort lzero)
   ax
   (dkind-ok/ax aProp/type-wf).

%% def letRed
%%% HOLE
letRed/type-wf : defeq (eapp (elam (esort (lsucc lzero)) ([x] x)) (esort lzero)) (eapp (elam (esort (lsucc lzero)) ([x] x)) (esort lzero)) (esort lzero).

%%% HOLE
letRed/value-typed : defeq (econst "aProp" lnil) (econst "aProp" lnil) (eapp (elam (esort (lsucc lzero)) ([x] x)) (esort lzero)).

letRed/decl : declared "letRed" lnil
   (eapp (elam (esort (lsucc lzero)) ([x] x)) (esort lzero))
   (defn (econst "aProp" lnil))
   (dkind-ok/defn letRed/type-wf letRed/value-typed).

