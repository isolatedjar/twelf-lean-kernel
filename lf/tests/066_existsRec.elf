%%% Expected outcome: accept
%%% Inductive predicates eliminate into Prop if they have one constructors and it carries data.
%% SKIP: inductive not yet supported
%% SKIP: def existsRec — universe-polymorphic (1 params)

%mode declared +N -LS -T -DK -W.
%worlds () (declared _ _ _ _ _).
%unique declared +N -1LS -1T -1DK *W.

%% Translator summary:
%%   - def existsRec: universe polymorphism not yet supported
