%%% Render-only view of: 120_dup_defs
%%% Two definitions with the same name
%% def dup_defs
dup_defs/render-type : expr =
   (esort (lsucc lzero)).
dup_defs/render-value : expr =
   (esort lzero).

%% def dup_defs
dup_defs/render-type : expr =
   (esort (lsucc lzero)).
dup_defs/render-value : expr =
   (esort lzero).

