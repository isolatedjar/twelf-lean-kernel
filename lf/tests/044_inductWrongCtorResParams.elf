%%% Expected outcome: reject
%%% An inductive with a constructor with wrong parameters in result (they are swapped)
%% SKIP: ctor inductWrongCtorResParams.mk — result-type param arg #1 should be bvar(1) (the param binder), got bvar(0)


%% Translator summary:
%%   - ctor inductWrongCtorResParams.mk: result-type param arg #1 should be bvar(1) (the param binder), got bvar(0)
