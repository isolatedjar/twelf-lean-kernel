%%% Expected outcome: reject
%%% An inductive type can have fields of from higher universes.
%% SKIP: ctor typeWithTooHighTypeField.mk — field #1 has type Sort 1 (which lives at Sort 2); inductive is at Sort 1, so the field's universe must be ≤ 0

%mode declared +N +LS -T -DK -W.
%worlds () (declared _ _ _ _ _).
%unique declared +N +LS -1T -1DK *W.

%% Translator summary:
%%   - ctor typeWithTooHighTypeField.mk: field #1 has type Sort 1 (which lives at Sort 2); inductive is at Sort 1, so the field's universe must be ≤ 0
