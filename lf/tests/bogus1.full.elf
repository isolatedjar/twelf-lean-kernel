%%% Expected outcome: reject
%%% A clearly bogus proof. Also serves as an example for how to write simple cases.
%solve nonneg_0 : 0 >= 0.
%solve nonneg_1 : 1 >= 0.

sneq/0 : string-neq "Nat.zero" "Nat.succ".
sneq/1 : string-neq "Nat" "OfNat".

%% inductive Eq
Eq/type-wf-sort : lvl
   = (limax
       (lsucc (lvar liz))
       (limax (lvar liz) (limax (lvar liz) (lsucc lzero)))).

Eq/type-wf : of (eforall (esort (lvar liz)) ([x] (eforall x ([y] (eforall x ([z] (esort lzero))))))) (esort Eq/type-wf-sort)
   = (defeq/forall
       defeq/sort-refl
       ([x]
         ([h]
           (defeq/forall
             h
             ([y] ([h1] (defeq/forall h ([z] ([h2] defeq/sort-refl))))))))).

Eq/ends-in-sort : ends-in-sort (eforall (esort (lvar liz)) ([x] (eforall x ([y] (eforall x ([z] (esort lzero)))))))
   = (ends-in-sort/forall
       ([x]
         (ends-in-sort/forall
           ([y] (ends-in-sort/forall ([z] ends-in-sort/sort)))))).

Eq/rec-name : name "Eq.rec" (is-rec-for "Eq").

Eq/eisl : ends-in-sort-with-level (eforall (esort (lvar liz)) ([x] (eforall x ([y] (eforall x ([z] (esort lzero))))))) lzero
   = (ends-in-sort-with-level/forall
       ([x]
         (ends-in-sort-with-level/forall
           ([y]
             (ends-in-sort-with-level/forall
               ([z] ends-in-sort-with-level/sort)))))).

Eq/cnd : cnames-distinct (ccons "Eq.refl" cnil)
   = (cnames-distinct/cons cnames-distinct/nil all-string-neq/nil).

Eq/name : name "Eq" (is-decl (eforall (esort (lvar liz)) ([x] (eforall x ([y] (eforall x ([z] (esort lzero))))))) (indt (ccons "Eq.refl" cnil) (lis (lis liz)))).

Eq/decl : declared "Eq" (eforall (esort (lvar liz)) ([x] (eforall x ([y] (eforall x ([z] (esort lzero))))))) (indt (ccons "Eq.refl" cnil) (lis (lis liz)))
   = declared/ok-indt Eq/name Eq/rec-name Eq/cnd (dkind-ok/indt Eq/type-wf Eq/ends-in-sort).

%% ctor Eq.refl (of Eq)
Eq_refl/type-wf-sort : lvl
   = (limax (lsucc (lvar liz)) (limax (lvar liz) lzero)).

Eq_refl/type-wf : of (eforall (esort (lvar liz)) ([x] (eforall x ([y] (eapp (eapp (eapp (econst "Eq" (lcons (lvar liz) lnil)) x) y) y))))) (esort Eq_refl/type-wf-sort)
   = (defeq/forall
       defeq/sort-refl
       ([x]
         ([h]
           (defeq/forall
             h
             ([y]
               ([h1]
                 (defeq/app
                   (defeq/app
                     (defeq/app
                       (defeq/const
                         Eq/decl
                         (inst-expr/forall
                           (inst-expr/sort (lvl-inst/var lvl-nth/zero))
                           ([x]
                             ([h]
                               (inst-expr/forall
                                 h
                                 ([y]
                                   ([h1]
                                     (inst-expr/forall
                                       h
                                       ([z]
                                         ([h2]
                                           (inst-expr/sort
                                             lvl-inst/zero)))))))))))
                       h)
                     h1)
                   h1))))))).

Eq_refl/positivity : ctor-positive "Eq" (lcons (lvar liz) lnil) (eforall (esort (lvar liz)) ([x] (eforall x ([y] (eapp (eapp (eapp (econst "Eq" (lcons (lvar liz) lnil)) x) y) y)))))
   = (ctor-positive/intro ([S] (eforall (esort (lvar liz)) ([x] (eforall x ([y] (eapp (eapp (eapp S x) y) y)))))) (ctor-spine/arg
       (strict-pos/no-occur (esort (lvar liz)) no-self-ref/sort)
       ([x]
         ([y]
           (ctor-spine/arg
             (strict-pos/no-occur x y)
             ([z]
               ([w]
                 (ctor-spine/result
                   (applies-self/app
                     (applies-self/app
                       (applies-self/app applies-self/refl))))))))))).

Eq_refl/cmem : cmem "Eq.refl" (ccons "Eq.refl" cnil)
   = cmem/here.

Eq_refl/fuo : field-universes-ok (eforall (esort (lvar liz)) ([x] (eforall x ([y] (eapp (eapp (eapp (econst "Eq" (lcons (lvar liz) lnil)) x) y) y))))) lzero
   = (field-universes-ok/forall
       defeq/sort-refl
       (mleq/imax-lzL (mleq/self nonneg_0))
       ([x]
         ([h]
           (field-universes-ok/forall
             h
             (mleq/imax-lzL (mleq/self nonneg_0))
             ([y] ([h1] (field-universes-ok/done not-forall/app))))))).

Eq_refl/name : name "Eq.refl" (is-decl (eforall (esort (lvar liz)) ([x] (eforall x ([y] (eapp (eapp (eapp (econst "Eq" (lcons (lvar liz) lnil)) x) y) y))))) (ctor "Eq" liz)).

Eq_refl/decl : declared "Eq.refl" (eforall (esort (lvar liz)) ([x] (eforall x ([y] (eapp (eapp (eapp (econst "Eq" (lcons (lvar liz) lnil)) x) y) y))))) (ctor "Eq" liz)
   = declared/ok-ctor Eq_refl/name Eq/name Eq_refl/cmem Eq/eisl Eq_refl/fuo (dkind-ok/ctor Eq_refl/type-wf Eq_refl/positivity).

%% recursor Eq.rec (TCB-derived; informational only)
%%   level params: u u_1
%%   type:   (eforall (esort (lvar (lis liz))) ([x] (eforall x ([y] (eforall (eforall x ([z] (eforall (eapp (eapp (eapp (econst "Eq" (lcons (lvar (lis liz)) lnil)) x) y) z) ([w] (esort (lvar liz)))))) ([z] (eforall (eapp (eapp z y) (eapp (eapp (econst "Eq.refl" (lcons (lvar (lis liz)) lnil)) x) y)) ([w] (eforall x ([x1] (eforall (eapp (eapp (eapp (econst "Eq" (lcons (lvar (lis liz)) lnil)) x) y) x1) ([y1] (eapp (eapp z x1) y1)))))))))))))
%%   rules:
%%     Eq.rec … Eq.refl … = (elam (esort (lvar (lis liz))) ([x] (elam x ([y] (elam (eforall x ([z] (eforall (eapp (eapp (eapp (econst "Eq" (lcons (lvar (lis liz)) lnil)) x) y) z) ([w] (esort (lvar liz)))))) ([z] (elam (eapp (eapp z y) (eapp (eapp (econst "Eq.refl" (lcons (lvar (lis liz)) lnil)) x) y)) ([w] w))))))))   (0 fields)
%%   K-like reduction: yes

%% inductive Nat
Nat/type-wf-sort : lvl
   = (lsucc (lsucc lzero)).

Nat/type-wf : of (esort (lsucc lzero)) (esort Nat/type-wf-sort)
   = defeq/sort-refl.

Nat/ends-in-sort : ends-in-sort (esort (lsucc lzero))
   = ends-in-sort/sort.

Nat/rec-name : name "Nat.rec" (is-rec-for "Nat").

Nat/eisl : ends-in-sort-with-level (esort (lsucc lzero)) (lsucc lzero)
   = ends-in-sort-with-level/sort.

Nat/cnd : cnames-distinct (ccons "Nat.zero" (ccons "Nat.succ" cnil))
   = (cnames-distinct/cons
       (cnames-distinct/cons cnames-distinct/nil all-string-neq/nil)
       (all-string-neq/cons sneq/0 all-string-neq/nil)).

Nat/name : name "Nat" (is-decl (esort (lsucc lzero)) (indt (ccons "Nat.zero" (ccons "Nat.succ" cnil)) liz)).

Nat/decl : declared "Nat" (esort (lsucc lzero)) (indt (ccons "Nat.zero" (ccons "Nat.succ" cnil)) liz)
   = declared/ok-indt Nat/name Nat/rec-name Nat/cnd (dkind-ok/indt Nat/type-wf Nat/ends-in-sort).

%% ctor Nat.zero (of Nat)
Nat_zero/type-wf-sort : lvl
   = (lsucc lzero).

Nat_zero/type-wf : of (econst "Nat" lnil) (esort Nat_zero/type-wf-sort)
   = (defeq/const Nat/decl (inst-expr/sort (lvl-inst/succ lvl-inst/zero))).

Nat_zero/positivity : ctor-positive "Nat" lnil (econst "Nat" lnil)
   = (ctor-positive/intro ([S] S) (ctor-spine/result applies-self/refl)).

Nat_zero/cmem : cmem "Nat.zero" (ccons "Nat.zero" (ccons "Nat.succ" cnil))
   = cmem/here.

Nat_zero/fuo : field-universes-ok (econst "Nat" lnil) (lsucc lzero)
   = (field-universes-ok/done not-forall/const).

Nat_zero/name : name "Nat.zero" (is-decl (econst "Nat" lnil) (ctor "Nat" liz)).

Nat_zero/decl : declared "Nat.zero" (econst "Nat" lnil) (ctor "Nat" liz)
   = declared/ok-ctor Nat_zero/name Nat/name Nat_zero/cmem Nat/eisl Nat_zero/fuo (dkind-ok/ctor Nat_zero/type-wf Nat_zero/positivity).

%% ctor Nat.succ (of Nat)
Nat_succ/type-wf-sort : lvl
   = (limax (lsucc lzero) (lsucc lzero)).

Nat_succ/type-wf : of (eforall (econst "Nat" lnil) ([x] (econst "Nat" lnil))) (esort Nat_succ/type-wf-sort)
   = (defeq/forall
       (defeq/const Nat/decl (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
       ([x]
         ([h]
           (defeq/const
             Nat/decl
             (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))))).

Nat_succ/positivity : ctor-positive "Nat" lnil (eforall (econst "Nat" lnil) ([x] (econst "Nat" lnil)))
   = (ctor-positive/intro ([S] (eforall S ([x] S))) (ctor-spine/arg
       (strict-pos/head applies-self/refl)
       ([x] ([y] (ctor-spine/result applies-self/refl))))).

Nat_succ/cmem : cmem "Nat.succ" (ccons "Nat.zero" (ccons "Nat.succ" cnil))
   = (cmem/there cmem/here).

Nat_succ/fuo : field-universes-ok (eforall (econst "Nat" lnil) ([x] (econst "Nat" lnil))) (lsucc lzero)
   = (field-universes-ok/forall
       (defeq/const Nat/decl (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
       (mleq/imax-lsL
         (mleq/sR
           (mleq/maxL
             (mleq/sL (mleq/self nonneg_0))
             (mleq/sL (mleq/self nonneg_0)))))
       ([x] ([h] (field-universes-ok/done not-forall/const)))).

Nat_succ/name : name "Nat.succ" (is-decl (eforall (econst "Nat" lnil) ([x] (econst "Nat" lnil))) (ctor "Nat" (lis liz))).

Nat_succ/decl : declared "Nat.succ" (eforall (econst "Nat" lnil) ([x] (econst "Nat" lnil))) (ctor "Nat" (lis liz))
   = declared/ok-ctor Nat_succ/name Nat/name Nat_succ/cmem Nat/eisl Nat_succ/fuo (dkind-ok/ctor Nat_succ/type-wf Nat_succ/positivity).

%% recursor Nat.rec (TCB-derived; informational only)
%%   level params: u
%%   type:   (eforall (eforall (econst "Nat" lnil) ([x] (esort (lvar liz)))) ([x] (eforall (eapp x (econst "Nat.zero" lnil)) ([y] (eforall (eforall (econst "Nat" lnil) ([z] (eforall (eapp x z) ([w] (eapp x (eapp (econst "Nat.succ" lnil) z)))))) ([z] (eforall (econst "Nat" lnil) ([w] (eapp x w)))))))))
%%   rules:
%%     Nat.rec … Nat.zero … = (elam (eforall (econst "Nat" lnil) ([x] (esort (lvar liz)))) ([x] (elam (eapp x (econst "Nat.zero" lnil)) ([y] (elam (eforall (econst "Nat" lnil) ([z] (eforall (eapp x z) ([w] (eapp x (eapp (econst "Nat.succ" lnil) z)))))) ([z] y))))))   (0 fields)
%%     Nat.rec … Nat.succ … = (elam (eforall (econst "Nat" lnil) ([x] (esort (lvar liz)))) ([x] (elam (eapp x (econst "Nat.zero" lnil)) ([y] (elam (eforall (econst "Nat" lnil) ([z] (eforall (eapp x z) ([w] (eapp x (eapp (econst "Nat.succ" lnil) z)))))) ([z] (elam (econst "Nat" lnil) ([w] (eapp (eapp z w) (eapp (eapp (eapp (eapp (econst "Nat.rec" (lcons (lvar liz) lnil)) x) y) z) w))))))))))   (1 fields)

%% inductive OfNat
OfNat/type-wf-sort : lvl
   = (limax
       (lsucc (lsucc (lvar liz)))
       (limax (lsucc lzero) (lsucc (lsucc (lvar liz))))).

OfNat/type-wf : of (eforall (esort (lsucc (lvar liz))) ([x] (eforall (econst "Nat" lnil) ([y] (esort (lsucc (lvar liz))))))) (esort OfNat/type-wf-sort)
   = (defeq/forall
       defeq/sort-refl
       ([x]
         ([h]
           (defeq/forall
             (defeq/const
               Nat/decl
               (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
             ([y] ([h1] defeq/sort-refl)))))).

OfNat/ends-in-sort : ends-in-sort (eforall (esort (lsucc (lvar liz))) ([x] (eforall (econst "Nat" lnil) ([y] (esort (lsucc (lvar liz)))))))
   = (ends-in-sort/forall ([x] (ends-in-sort/forall ([y] ends-in-sort/sort)))).

OfNat/rec-name : name "OfNat.rec" (is-rec-for "OfNat").

OfNat/eisl : ends-in-sort-with-level (eforall (esort (lsucc (lvar liz))) ([x] (eforall (econst "Nat" lnil) ([y] (esort (lsucc (lvar liz))))))) (lsucc (lvar liz))
   = (ends-in-sort-with-level/forall
       ([x]
         (ends-in-sort-with-level/forall ([y] ends-in-sort-with-level/sort)))).

OfNat/cnd : cnames-distinct (ccons "OfNat.mk" cnil)
   = (cnames-distinct/cons cnames-distinct/nil all-string-neq/nil).

OfNat/name : name "OfNat" (is-decl (eforall (esort (lsucc (lvar liz))) ([x] (eforall (econst "Nat" lnil) ([y] (esort (lsucc (lvar liz))))))) (indt (ccons "OfNat.mk" cnil) (lis (lis liz)))).

OfNat/decl : declared "OfNat" (eforall (esort (lsucc (lvar liz))) ([x] (eforall (econst "Nat" lnil) ([y] (esort (lsucc (lvar liz))))))) (indt (ccons "OfNat.mk" cnil) (lis (lis liz)))
   = declared/ok-indt OfNat/name OfNat/rec-name OfNat/cnd (dkind-ok/indt OfNat/type-wf OfNat/ends-in-sort).

%% ctor OfNat.mk (of OfNat)
OfNat_mk/type-wf-sort : lvl
   = (limax
       (lsucc (lsucc (lvar liz)))
       (limax (lsucc lzero) (limax (lsucc (lvar liz)) (lsucc (lvar liz))))).

OfNat_mk/type-wf : of (eforall (esort (lsucc (lvar liz))) ([x] (eforall (econst "Nat" lnil) ([y] (eforall x ([z] (eapp (eapp (econst "OfNat" (lcons (lvar liz) lnil)) x) y))))))) (esort OfNat_mk/type-wf-sort)
   = (defeq/forall
       defeq/sort-refl
       ([x]
         ([h]
           (defeq/forall
             (defeq/const
               Nat/decl
               (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
             ([y]
               ([h1]
                 (defeq/forall
                   h
                   ([z]
                     ([h2]
                       (defeq/app
                         (defeq/app
                           (defeq/const
                             OfNat/decl
                             (inst-expr/forall
                               (inst-expr/sort
                                 (lvl-inst/succ (lvl-inst/var lvl-nth/zero)))
                               ([x]
                                 ([h]
                                   (inst-expr/forall
                                     (inst-expr/const lvls-inst/nil)
                                     ([y]
                                       ([h1]
                                         (inst-expr/sort
                                           (lvl-inst/succ
                                             (lvl-inst/var
                                               lvl-nth/zero))))))))))
                           h)
                         h1)))))))))).

OfNat_mk/positivity : ctor-positive "OfNat" (lcons (lvar liz) lnil) (eforall (esort (lsucc (lvar liz))) ([x] (eforall (econst "Nat" lnil) ([y] (eforall x ([z] (eapp (eapp (econst "OfNat" (lcons (lvar liz) lnil)) x) y)))))))
   = (ctor-positive/intro ([S] (eforall (esort (lsucc (lvar liz))) ([x] (eforall (econst "Nat" lnil) ([y] (eforall x ([z] (eapp (eapp S x) y)))))))) (ctor-spine/arg
       (strict-pos/no-occur (esort (lsucc (lvar liz))) no-self-ref/sort)
       ([x]
         ([y]
           (ctor-spine/arg
             (strict-pos/no-occur
               (econst "Nat" lnil)
               (no-self-ref/const sneq/1))
             ([z]
               ([w]
                 (ctor-spine/arg
                   (strict-pos/no-occur x y)
                   ([x1]
                     ([y1]
                       (ctor-spine/result
                         (applies-self/app
                           (applies-self/app applies-self/refl))))))))))))).

OfNat_mk/cmem : cmem "OfNat.mk" (ccons "OfNat.mk" cnil)
   = cmem/here.

%%% HOLE: OfNat_mk/fuo
OfNat_mk/fuo : field-universes-ok (eforall (esort (lsucc (lvar liz))) ([x] (eforall (econst "Nat" lnil) ([y] (eforall x ([z] (eapp (eapp (econst "OfNat" (lcons (lvar liz) lnil)) x) y))))))) (lsucc (lvar liz)).

OfNat_mk/name : name "OfNat.mk" (is-decl (eforall (esort (lsucc (lvar liz))) ([x] (eforall (econst "Nat" lnil) ([y] (eforall x ([z] (eapp (eapp (econst "OfNat" (lcons (lvar liz) lnil)) x) y))))))) (ctor "OfNat" liz)).

OfNat_mk/decl : declared "OfNat.mk" (eforall (esort (lsucc (lvar liz))) ([x] (eforall (econst "Nat" lnil) ([y] (eforall x ([z] (eapp (eapp (econst "OfNat" (lcons (lvar liz) lnil)) x) y))))))) (ctor "OfNat" liz)
   = declared/ok-ctor OfNat_mk/name OfNat/name OfNat_mk/cmem OfNat/eisl OfNat_mk/fuo (dkind-ok/ctor OfNat_mk/type-wf OfNat_mk/positivity).

%% recursor OfNat.rec (TCB-derived; informational only)
%%   level params: u_1 u
%%   type:   (eforall (esort (lsucc (lvar (lis liz)))) ([x] (eforall (econst "Nat" lnil) ([y] (eforall (eforall (eapp (eapp (econst "OfNat" (lcons (lvar (lis liz)) lnil)) x) y) ([z] (esort (lvar liz)))) ([z] (eforall (eforall x ([w] (eapp z (eapp (eapp (eapp (econst "OfNat.mk" (lcons (lvar (lis liz)) lnil)) x) y) w)))) ([w] (eforall (eapp (eapp (econst "OfNat" (lcons (lvar (lis liz)) lnil)) x) y) ([x1] (eapp z x1)))))))))))
%%   rules:
%%     OfNat.rec … OfNat.mk … = (elam (esort (lsucc (lvar (lis liz)))) ([x] (elam (econst "Nat" lnil) ([y] (elam (eforall (eapp (eapp (econst "OfNat" (lcons (lvar (lis liz)) lnil)) x) y) ([z] (esort (lvar liz)))) ([z] (elam (eforall x ([w] (eapp z (eapp (eapp (eapp (econst "OfNat.mk" (lcons (lvar (lis liz)) lnil)) x) y) w)))) ([w] (elam x ([x1] (eapp w x1)))))))))))   (1 fields)

%% def OfNat.ofNat
OfNat_ofNat/type-wf-sort : lvl
   = (limax
       (lsucc (lsucc (lvar liz)))
       (limax (lsucc lzero) (limax (lsucc (lvar liz)) (lsucc (lvar liz))))).

OfNat_ofNat/type-wf : of (eforall (esort (lsucc (lvar liz))) ([x] (eforall (econst "Nat" lnil) ([y] (eforall (eapp (eapp (econst "OfNat" (lcons (lvar liz) lnil)) x) y) ([z] x)))))) (esort OfNat_ofNat/type-wf-sort)
   = (defeq/forall
       defeq/sort-refl
       ([x]
         ([h]
           (defeq/forall
             (defeq/const
               Nat/decl
               (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
             ([y]
               ([h1]
                 (defeq/forall
                   (defeq/app
                     (defeq/app
                       (defeq/const
                         OfNat/decl
                         (inst-expr/forall
                           (inst-expr/sort
                             (lvl-inst/succ (lvl-inst/var lvl-nth/zero)))
                           ([x]
                             ([h]
                               (inst-expr/forall
                                 (inst-expr/const lvls-inst/nil)
                                 ([y]
                                   ([h1]
                                     (inst-expr/sort
                                       (lvl-inst/succ
                                         (lvl-inst/var lvl-nth/zero))))))))))
                       h)
                     h1)
                   ([z] ([h2] h))))))))).

%%% HOLE
OfNat_ofNat/value-typed : of
  (elam (esort (lsucc (lvar liz))) ([x] (elam (econst "Nat" lnil) ([y] (elam (eapp (eapp (econst "OfNat" (lcons (lvar liz) lnil)) x) y) ([z] (eproj "OfNat" 0 z)))))))
  (eforall (esort (lsucc (lvar liz))) ([x] (eforall (econst "Nat" lnil) ([y] (eforall (eapp (eapp (econst "OfNat" (lcons (lvar liz) lnil)) x) y) ([z] x)))))).

OfNat_ofNat/name : name "OfNat.ofNat" (is-decl
  (eforall (esort (lsucc (lvar liz))) ([x] (eforall (econst "Nat" lnil) ([y] (eforall (eapp (eapp (econst "OfNat" (lcons (lvar liz) lnil)) x) y) ([z] x))))))
  (defn (elam (esort (lsucc (lvar liz))) ([x] (elam (econst "Nat" lnil) ([y] (elam (eapp (eapp (econst "OfNat" (lcons (lvar liz) lnil)) x) y) ([z] (eproj "OfNat" 0 z))))))))).

OfNat_ofNat/decl : declared "OfNat.ofNat"
  (eforall (esort (lsucc (lvar liz))) ([x] (eforall (econst "Nat" lnil) ([y] (eforall (eapp (eapp (econst "OfNat" (lcons (lvar liz) lnil)) x) y) ([z] x))))))
  (defn (elam (esort (lsucc (lvar liz))) ([x] (elam (econst "Nat" lnil) ([y] (elam (eapp (eapp (econst "OfNat" (lcons (lvar liz) lnil)) x) y) ([z] (eproj "OfNat" 0 z))))))))
   = declared/ok OfNat_ofNat/name dkind-non-inductive/defn (dkind-ok/defn OfNat_ofNat/type-wf OfNat_ofNat/value-typed).

%% def instOfNatNat
instOfNatNat/type-wf-sort : lvl
   = (limax (lsucc lzero) (lsucc lzero)).

instOfNatNat/type-wf : of (eforall (econst "Nat" lnil) ([x] (eapp (eapp (econst "OfNat" (lcons lzero lnil)) (econst "Nat" lnil)) x))) (esort instOfNatNat/type-wf-sort)
   = (defeq/forall
       (defeq/const Nat/decl (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
       ([x]
         ([h]
           (defeq/app
             (defeq/app
               (defeq/const
                 OfNat/decl
                 (inst-expr/forall
                   (inst-expr/sort (lvl-inst/succ (lvl-inst/var lvl-nth/zero)))
                   ([x]
                     ([h]
                       (inst-expr/forall
                         (inst-expr/const lvls-inst/nil)
                         ([y]
                           ([h1]
                             (inst-expr/sort
                               (lvl-inst/succ
                                 (lvl-inst/var lvl-nth/zero))))))))))
               (defeq/const
                 Nat/decl
                 (inst-expr/sort (lvl-inst/succ lvl-inst/zero))))
             h)))).

instOfNatNat/value-typed : of
  (elam (econst "Nat" lnil) ([x] (eapp (eapp (eapp (econst "OfNat.mk" (lcons lzero lnil)) (econst "Nat" lnil)) x) x)))
  (eforall (econst "Nat" lnil) ([x] (eapp (eapp (econst "OfNat" (lcons lzero lnil)) (econst "Nat" lnil)) x)))
   = (defeq/lam
       (defeq/const Nat/decl (inst-expr/sort (lvl-inst/succ lvl-inst/zero)))
       ([x]
         ([h]
           (defeq/app
             (defeq/app
               (defeq/app
                 (defeq/const
                   OfNat_mk/decl
                   (inst-expr/forall
                     (inst-expr/sort
                       (lvl-inst/succ (lvl-inst/var lvl-nth/zero)))
                     ([x]
                       ([h]
                         (inst-expr/forall
                           (inst-expr/const lvls-inst/nil)
                           ([y]
                             ([h1]
                               (inst-expr/forall
                                 h
                                 ([z]
                                   ([h2]
                                     (inst-expr/app
                                       (inst-expr/app
                                         (inst-expr/const
                                           (lvls-inst/cons
                                             (lvl-inst/var lvl-nth/zero)
                                             lvls-inst/nil))
                                         h)
                                       h1)))))))))))
                 (defeq/const
                   Nat/decl
                   (inst-expr/sort (lvl-inst/succ lvl-inst/zero))))
               h)
             h)))).

instOfNatNat/name : name "instOfNatNat" (is-decl
  (eforall (econst "Nat" lnil) ([x] (eapp (eapp (econst "OfNat" (lcons lzero lnil)) (econst "Nat" lnil)) x)))
  (defn (elam (econst "Nat" lnil) ([x] (eapp (eapp (eapp (econst "OfNat.mk" (lcons lzero lnil)) (econst "Nat" lnil)) x) x))))).

instOfNatNat/decl : declared "instOfNatNat"
  (eforall (econst "Nat" lnil) ([x] (eapp (eapp (econst "OfNat" (lcons lzero lnil)) (econst "Nat" lnil)) x)))
  (defn (elam (econst "Nat" lnil) ([x] (eapp (eapp (eapp (econst "OfNat.mk" (lcons lzero lnil)) (econst "Nat" lnil)) x) x))))
   = declared/ok instOfNatNat/name dkind-non-inductive/defn (dkind-ok/defn instOfNatNat/type-wf instOfNatNat/value-typed).

%% inductive True
True/type-wf-sort : lvl
   = (lsucc lzero).

True/type-wf : of (esort lzero) (esort True/type-wf-sort)
   = defeq/sort-refl.

True/ends-in-sort : ends-in-sort (esort lzero)
   = ends-in-sort/sort.

True/rec-name : name "True.rec" (is-rec-for "True").

True/eisl : ends-in-sort-with-level (esort lzero) lzero
   = ends-in-sort-with-level/sort.

True/cnd : cnames-distinct (ccons "True.intro" cnil)
   = (cnames-distinct/cons cnames-distinct/nil all-string-neq/nil).

True/name : name "True" (is-decl (esort lzero) (indt (ccons "True.intro" cnil) liz)).

True/decl : declared "True" (esort lzero) (indt (ccons "True.intro" cnil) liz)
   = declared/ok-indt True/name True/rec-name True/cnd (dkind-ok/indt True/type-wf True/ends-in-sort).

%% ctor True.intro (of True)
True_intro/type-wf-sort : lvl
   = lzero.

True_intro/type-wf : of (econst "True" lnil) (esort True_intro/type-wf-sort)
   = (defeq/const True/decl (inst-expr/sort lvl-inst/zero)).

True_intro/positivity : ctor-positive "True" lnil (econst "True" lnil)
   = (ctor-positive/intro ([S] S) (ctor-spine/result applies-self/refl)).

True_intro/cmem : cmem "True.intro" (ccons "True.intro" cnil)
   = cmem/here.

True_intro/fuo : field-universes-ok (econst "True" lnil) lzero
   = (field-universes-ok/done not-forall/const).

True_intro/name : name "True.intro" (is-decl (econst "True" lnil) (ctor "True" liz)).

True_intro/decl : declared "True.intro" (econst "True" lnil) (ctor "True" liz)
   = declared/ok-ctor True_intro/name True/name True_intro/cmem True/eisl True_intro/fuo (dkind-ok/ctor True_intro/type-wf True_intro/positivity).

%% recursor True.rec (TCB-derived; informational only)
%%   level params: u
%%   type:   (eforall (eforall (econst "True" lnil) ([x] (esort (lvar liz)))) ([x] (eforall (eapp x (econst "True.intro" lnil)) ([y] (eforall (econst "True" lnil) ([z] (eapp x z)))))))
%%   rules:
%%     True.rec … True.intro … = (elam (eforall (econst "True" lnil) ([x] (esort (lvar liz)))) ([x] (elam (eapp x (econst "True.intro" lnil)) ([y] y))))   (0 fields)
%%   K-like reduction: yes

%% thm thm
%%% HOLE
thm/type-wf : of
  (eapp (eapp (eapp (econst "Eq" (lcons (lsucc lzero) lnil)) (econst "Nat" lnil))
    (eapp (eapp (eapp (econst "OfNat.ofNat" (lcons lzero lnil)) (econst "Nat" lnil)) (enatlit 0 nonneg_0)) (eapp (econst "instOfNatNat" lnil) (enatlit 0 nonneg_0))))
    (eapp (eapp (eapp (econst "OfNat.ofNat" (lcons lzero lnil)) (econst "Nat" lnil)) (enatlit 1 nonneg_1)) (eapp (econst "instOfNatNat" lnil) (enatlit 1 nonneg_1))))
  (esort lzero).

%%% HOLE
thm/value-typed : of
  (econst "True.intro" lnil)
  (eapp (eapp (eapp (econst "Eq" (lcons (lsucc lzero) lnil)) (econst "Nat" lnil))
    (eapp (eapp (eapp (econst "OfNat.ofNat" (lcons lzero lnil)) (econst "Nat" lnil)) (enatlit 0 nonneg_0)) (eapp (econst "instOfNatNat" lnil) (enatlit 0 nonneg_0))))
    (eapp (eapp (eapp (econst "OfNat.ofNat" (lcons lzero lnil)) (econst "Nat" lnil)) (enatlit 1 nonneg_1)) (eapp (econst "instOfNatNat" lnil) (enatlit 1 nonneg_1)))).

thm/name : name "thm" (is-decl
  (eapp (eapp (eapp (econst "Eq" (lcons (lsucc lzero) lnil)) (econst "Nat" lnil))
    (eapp (eapp (eapp (econst "OfNat.ofNat" (lcons lzero lnil)) (econst "Nat" lnil)) (enatlit 0 nonneg_0)) (eapp (econst "instOfNatNat" lnil) (enatlit 0 nonneg_0))))
    (eapp (eapp (eapp (econst "OfNat.ofNat" (lcons lzero lnil)) (econst "Nat" lnil)) (enatlit 1 nonneg_1)) (eapp (econst "instOfNatNat" lnil) (enatlit 1 nonneg_1))))
  (thm (econst "True.intro" lnil))).

thm/decl : declared "thm"
  (eapp (eapp (eapp (econst "Eq" (lcons (lsucc lzero) lnil)) (econst "Nat" lnil))
    (eapp (eapp (eapp (econst "OfNat.ofNat" (lcons lzero lnil)) (econst "Nat" lnil)) (enatlit 0 nonneg_0)) (eapp (econst "instOfNatNat" lnil) (enatlit 0 nonneg_0))))
    (eapp (eapp (eapp (econst "OfNat.ofNat" (lcons lzero lnil)) (econst "Nat" lnil)) (enatlit 1 nonneg_1)) (eapp (econst "instOfNatNat" lnil) (enatlit 1 nonneg_1))))
  (thm (econst "True.intro" lnil))
   = declared/ok thm/name dkind-non-inductive/thm (dkind-ok/thm thm/type-wf thm/value-typed).
