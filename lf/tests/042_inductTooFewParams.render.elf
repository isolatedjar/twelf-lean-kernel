%%% Render-only view of: 042_inductTooFewParams
%%% An inductive with too few parameters in the type
%% inductive inductTooFewParams (numParams=2, numIndices=0)
inductTooFewParams/render-type : expr =
   (eforall (esort lzero) ([x] (esort lzero))).

