%%% Render-only view of: 017_levelComp4
%%% Some level computation
%% def levelComp4
%%% HOLE
levelComp4/type-wf-sort : {u : lvl} lvl.

%%% HOLE
levelComp4/type-wf : {u : lvl} defeq (esort (lsucc lzero)) (esort (lsucc lzero)) (esort (levelComp4/type-wf-sort u)).

%%% HOLE
levelComp4/value-typed : {u : lvl} defeq (esort lzero) (esort lzero) (esort (lsucc lzero)).

levelComp4/name : {u : lvl} name "levelComp4" (is-decl (lcons u lnil) (esort (lsucc lzero)) (defn (esort lzero))).

levelComp4/decl : {u : lvl} declared "levelComp4" (lcons u lnil)
   (esort (lsucc lzero))
   (defn (esort lzero))
   (levelComp4/name u)
   (dkind-ok/defn (levelComp4/type-wf u) (levelComp4/value-typed u)).

