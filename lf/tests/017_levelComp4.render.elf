%%% Render-only view of: 017_levelComp4
%%% Some level computation
%% def levelComp4
%%% HOLE
levelComp4/type-wf : of (esort (lsucc lzero)) (esort _).

%%% HOLE
levelComp4/value-typed : of (esort lzero) (esort (lsucc lzero)).

levelComp4/name : name "levelComp4" (is-decl (esort (lsucc lzero)) (defn (esort lzero))).

levelComp4/decl : declared "levelComp4" (esort (lsucc lzero)) (defn (esort lzero))
   = declared/ok levelComp4/name dkind-non-inductive/defn (dkind-ok/defn levelComp4/type-wf levelComp4/value-typed).
