%%% Expected outcome: accept
%%% Asserting the type of `Acc.rec`.
%% SKIP: inductive not yet supported
%% SKIP: def accRecType — synth failed: const "Acc" not in decl table

%mode declared +N +LS -T -DK -W.
%worlds () (declared _ _ _ _ _).
%unique declared +N +LS -1T -1DK *W.

%% Translator summary:
%%   - def accRecType: synth failed (const "Acc" not in decl table)
