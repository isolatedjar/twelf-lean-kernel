%%% Render-only view of: 020_imax2
%%% Type inference for forall using imax
%% def imax2
imax2/render-type : expr =
   (eforall (esort (lsucc lzero)) ([x] (esort (lsucc (lsucc lzero))))).
imax2/render-value : expr =
   (elam (esort (lsucc lzero)) ([x] (eforall (esort (lsucc lzero)) ([y] x)))).

