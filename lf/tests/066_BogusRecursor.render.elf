%%% Render-only view of: 066_BogusRecursor
%%% A kernel must validate the recursors it is handed. If we write
%%% 
%%% ```
%%% inductive BogusRecursor : Type where
%%%   | mk : BogusRecursor
%%% ```
%%% 
%%% then the recursor `BogusRecursor.rec` will be correctly derived with type
%%% `{motive : BogusRecursor → Sort u} → motive .mk → (t : BogusRecursor) → motive t`.
%%% 
%%% This test claims the recursor for `BogusRecursor` should be a trivial constant
%%% of type `Prop`. A correct kernel notices the discrepancy and rejects.
%% inductive BogusRecursor
%%% HOLE
BogusRecursor/type-wf-sort : lvl.

%%% HOLE
BogusRecursor/type-wf : of (esort (lsucc lzero)) (esort BogusRecursor/type-wf-sort).

%%% HOLE
BogusRecursor/ends-in-sort : ends-in-sort (esort (lsucc lzero)).

BogusRecursor/rec-name : name "BogusRecursor.rec" (is-rec-for "BogusRecursor").

BogusRecursor/clf : count-leading-foralls (esort (lsucc lzero)) liz
   = count-leading-foralls/zero.

BogusRecursor/cnd : cnames-distinct (ccons "BogusRecursor.mk" cnil)
   = (cnames-distinct/cons cnames-distinct/nil all-string-neq/nil).

BogusRecursor/name : name "BogusRecursor" (is-decl (esort (lsucc lzero)) (indt (ccons "BogusRecursor.mk" cnil) liz)).

BogusRecursor/decl : declared "BogusRecursor" (esort (lsucc lzero)) (indt (ccons "BogusRecursor.mk" cnil) liz)
   = declared/ok-indt BogusRecursor/name BogusRecursor/rec-name BogusRecursor/clf BogusRecursor/cnd (dkind-ok/indt BogusRecursor/type-wf BogusRecursor/ends-in-sort).

%% ctor BogusRecursor.mk (of BogusRecursor)
%%% HOLE
BogusRecursor_mk/type-wf-sort : lvl.

%%% HOLE
BogusRecursor_mk/type-wf : of (econst "BogusRecursor" lnil) (esort BogusRecursor_mk/type-wf-sort).

%%% HOLE
BogusRecursor_mk/positivity : ctor-positive "BogusRecursor" lnil (econst "BogusRecursor" lnil).

BogusRecursor_mk/eisl : ends-in-sort-with-level (esort (lsucc lzero)) (lsucc lzero)
   = ends-in-sort-with-level/sort.

BogusRecursor_mk/cmem : cmem "BogusRecursor.mk" (ccons "BogusRecursor.mk" cnil)
   = cmem/here.

%%% HOLE: BogusRecursor_mk/fuo
BogusRecursor_mk/fuo : field-universes-ok-skip-params liz (econst "BogusRecursor" lnil) (lsucc lzero).

BogusRecursor_mk/name : name "BogusRecursor.mk" (is-decl (econst "BogusRecursor" lnil) (ctor "BogusRecursor" liz)).

BogusRecursor_mk/decl : declared "BogusRecursor.mk" (econst "BogusRecursor" lnil) (ctor "BogusRecursor" liz)
   = declared/ok-ctor BogusRecursor_mk/name BogusRecursor/name BogusRecursor_mk/cmem BogusRecursor_mk/eisl BogusRecursor_mk/fuo (dkind-ok/ctor BogusRecursor_mk/type-wf BogusRecursor_mk/positivity).

%% recursor BogusRecursor.rec
%%% HOLE
BogusRecursor_rec/type-wf-sort : lvl.

%%% HOLE
BogusRecursor_rec/type-wf : of (esort lzero) (esort BogusRecursor_rec/type-wf-sort).

BogusRecursor_rec/decl : declared "BogusRecursor.rec" (esort lzero) (irec "BogusRecursor")
   = declared/ok-irec BogusRecursor/rec-name (dkind-ok/irec BogusRecursor_rec/type-wf).
