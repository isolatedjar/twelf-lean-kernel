%%% Render-only view of: 011_nonPropThm
%%% The type of a theorem has to be a proposition
%% thm nonPropThm
nonPropThm/render-type : expr =
   (esort lzero).
nonPropThm/render-value : expr =
   (eforall (esort lzero) ([x] x)).

