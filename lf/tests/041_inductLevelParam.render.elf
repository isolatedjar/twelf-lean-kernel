%%% Render-only view of: 041_inductLevelParam
%%% An inductive with duplicate level params
%% inductive inductLevelParam (numParams=0, numIndices=0)
inductLevelParam/render-type : {u : lvl} {u : lvl} expr =
   [u] [u] (esort (lsucc lzero)).

