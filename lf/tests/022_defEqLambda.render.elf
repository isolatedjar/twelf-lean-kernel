%%% Render-only view of: 022_defEqLambda
%%% Definitional equality between lambdas
%% def defEqLambda
%%% HOLE
defEqLambda/type-wf-sort : lvl.

%%% HOLE
defEqLambda/type-wf : of (eforall (eforall (eforall (esort lzero) ([x] (esort lzero))) ([x] (esort lzero)))
                        ([x]
                          (eforall (eforall (eforall (esort lzero) ([y] (esort lzero))) ([y] (eapp x y)))
                            ([y] (eapp x (elam (esort lzero) ([z] (eforall z ([w] z))))))))) (esort defEqLambda/type-wf-sort).

%%% HOLE
defEqLambda/value-typed : of (elam (eforall (eforall (esort lzero) ([x] (esort lzero))) ([x] (esort lzero)))
                            ([x]
                              (elam (eforall (eforall (esort lzero) ([y] (esort lzero))) ([y] (eapp x y)))
                                ([y]
                                  (eapp y
                                    (elam (esort lzero)
                                      ([z]
                                        (eforall z
                                          ([w]
                                            z))))))))) (eforall (eforall (eforall (esort lzero) ([x] (esort lzero))) ([x] (esort lzero)))
                            ([x] (eforall (eforall (eforall (esort lzero) ([y] (esort lzero))) ([y] (eapp x y))) ([y] (eapp x (elam (esort lzero) ([z] (eforall z ([w] z))))))))).

defEqLambda/name : name "defEqLambda" (is-decl (eforall (eforall (eforall (esort lzero) ([x] (esort lzero))) ([x] (esort lzero)))
                                                 ([x] (eforall (eforall (eforall (esort lzero) ([y] (esort lzero))) ([y] (eapp x y))) ([y] (eapp x (elam (esort lzero) ([z] (eforall z ([w] z))))))))) (defn (elam (eforall (eforall (esort lzero) ([x] (esort lzero))) ([x] (esort lzero))) ([x] (elam (eforall (eforall (esort lzero) ([y] (esort lzero))) ([y] (eapp x y))) ([y] (eapp y (elam (esort lzero) ([z] (eforall z ([w] z))))))))))).

defEqLambda/decl : declared "defEqLambda" (eforall (eforall (eforall (esort lzero) ([x] (esort lzero))) ([x] (esort lzero)))
                                            ([x] (eforall (eforall (eforall (esort lzero) ([y] (esort lzero))) ([y] (eapp x y))) ([y] (eapp x (elam (esort lzero) ([z] (eforall z ([w] z))))))))) (defn (elam (eforall (eforall (esort lzero) ([x] (esort lzero))) ([x] (esort lzero))) ([x] (elam (eforall (eforall (esort lzero) ([y] (esort lzero))) ([y] (eapp x y))) ([y] (eapp y (elam (esort lzero) ([z] (eforall z ([w] z))))))))))
   = declared/ok defEqLambda/name (dkind-ok/defn defEqLambda/type-wf defEqLambda/value-typed).
