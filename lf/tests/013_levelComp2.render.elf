%%% Render-only view of: 013_levelComp2
%%% Some level computation
%% def levelComp2
%%% HOLE
levelComp2/type-wf : of (esort (lsucc (lsucc lzero))) (esort _).

%%% HOLE
levelComp2/value-typed : of (esort (limax lzero (lsucc lzero))) (esort (lsucc (lsucc lzero))).

levelComp2/name : name "levelComp2" (is-decl (esort (lsucc (lsucc lzero))) (defn (esort (limax lzero (lsucc lzero))))).

levelComp2/decl : declared "levelComp2" (esort (lsucc (lsucc lzero))) (defn (esort (limax lzero (lsucc lzero))))
   = declared/ok levelComp2/name dkind-non-inductive/defn (dkind-ok/defn levelComp2/type-wf levelComp2/value-typed).
