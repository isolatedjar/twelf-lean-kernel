%%% Render-only view of: 013_levelComp2
%%% Some level computation
%% def levelComp2
levelComp2/render-type : expr =
   (esort (lsucc (lsucc lzero))).
levelComp2/render-value : expr =
   (esort (limax lzero (lsucc lzero))).

