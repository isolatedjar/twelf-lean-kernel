%%% Render-only view of: 003_arrowType
%%% Arrow type (function type)
%% def arrowType
arrowType/render-type : expr =
   (esort (lsucc lzero)).
arrowType/render-value : expr =
   (eforall (esort lzero) ([x] (esort lzero))).

