%%% Render-only view of: 003_arrowType
%%% Arrow type (function type)
%% def arrowType
%%% HOLE
arrowType/type-wf-sort : lvl.

%%% HOLE
arrowType/type-wf : defeq (esort (lsucc lzero)) (esort (lsucc lzero)) (esort arrowType/type-wf-sort).

%%% HOLE
arrowType/value-typed : defeq (eforall (esort lzero) ([x] (esort lzero))) (eforall (esort lzero) ([x] (esort lzero))) (esort (lsucc lzero)).

arrowType/name : name "arrowType" (is-decl lnil (esort (lsucc lzero)) (defn (eforall (esort lzero) ([x] (esort lzero))))).

arrowType/decl : declared "arrowType" lnil
   (esort (lsucc lzero))
   (defn (eforall (esort lzero) ([x] (esort lzero))))
   arrowType/name
   (dkind-ok/defn arrowType/type-wf arrowType/value-typed).

